# TASK-0018 — Claude QA Review: v4 Supabase Migration SQL and RLS

**Task ID:** TASK-0018  
**Title:** Claude QA Review — v4 Supabase Migration SQL and RLS Test Plan  
**Owner:** Claude  
**Status:** Done — 2026-05-27  
**Codex QA:** Approved — 2026-05-27 (`docs/architecture/CODEX_QA_TASK_0018_RESPONSE.md`)  
**Cleanup Status:** Resolved — 2026-05-27  
**Priority:** High  
**Created Date:** 2026-05-27  
**Related Tasks:** TASK-0007, TASK-0011, TASK-0012, TASK-0013, TASK-0014, TASK-0015, TASK-0020  

---

## Context

The v4 Supabase migration was applied to `wvzjfxacykgsaffskgtr` on 2026-05-26 and later remediated through R4 plus the selfie Storage foundation.

Current migration chain:

| Migration | Applied | Key Changes |
|---|---|---|
| `v4_passto_mvp_schema` | 2026-05-26 | 15 tables, RLS, indexes, 37 seed rows, SECURITY DEFINER RPCs |
| `v4_passto_mvp_remediation_r1` | 2026-05-26 | search_path fix, anon EXECUTE revokes, profiles RLS initplan fix, 4 FK indexes |
| `v4_passto_mvp_remediation_r2` | 2026-05-26 | PUBLIC EXECUTE revoke, explicit authenticated grants |
| `v4_passto_mvp_remediation_r3` | 2026-05-26 | subscriptions ON DELETE RESTRICT, token revoke RPC, Pending status group |
| `v4_passto_mvp_selfie_storage` | 2026-05-27 | private `selfies` bucket, exact-path Storage policies, profile selfie fields |
| `v4_passto_mvp_remediation_r4` | 2026-05-27 | `licenses.normalized_status` expanded to include `Pending` |

TASK-0018 was a fresh, independent Claude review of the complete migration artifact at that time. No migration was applied as part of the TASK-0018 review itself.

---

## Overall QA Result

**PASS WITH REQUIRED FIX — resolved after R4**

Claude identified one P1 finding and several P2/P3 cleanup items. Codex confirmed the P1 and approved R4. R4 has now been applied and verified.

---

## Findings and Resolution Status

| Priority | Finding | Resolution |
|---|---|---|
| P1 | `licenses.normalized_status` did not include `Pending` while `license_status_mappings` did. | Resolved by `v4_passto_mvp_remediation_r4`; documented in `docs/architecture/V4_REMEDIATION_R4_LICENSE_PENDING.md`. |
| P2 | `stripe_customers` deviation not formally documented. | Resolved by `docs/architecture/ADR/ADR-0001-stripe-customer-mapping.md`. |
| P2 | PRD Section 4 listed stale `stripe_customers` and treated `show_qr` as simply deferred despite schema support. | Resolved in `docs/prd/PRD_SECTION_04_DATA_RLS_BACKEND.md`. |
| P2 | Build sequence lacked explicit `show_qr` implementation guard. | Resolved in `docs/tasks/MVP_LAUNCH_CRITICAL_BUILD_SEQUENCE.md`. |
| P2 | `V4_MIGRATION_VERIFICATION.md` Section 5 seed-data table was stale. | Resolved in `docs/architecture/V4_MIGRATION_VERIFICATION.md`. |
| P3 | Hard-delete cascade retained risk needed clearer security note. | Resolved in `docs/architecture/SECURITY_MODEL.md`. |
| P3 | Selfie Storage bucket not yet created. | Resolved by TASK-0020 and verified by Codex QA. |

---

## P1 Details — Resolved

R3 expanded `license_status_mappings.normalized_status` to 8 values including `Pending`, but `licenses.normalized_status` still allowed only 7 values.

R4 applied this fix:

```sql
alter table public.licenses
  drop constraint if exists licenses_normalized_status_check;

alter table public.licenses
  add constraint licenses_normalized_status_check
  check (normalized_status = any (array[
    'Active','Inactive','Expired','Surrendered',
    'Revoked','Suspended','Pending','Unknown'
  ]));

comment on column public.licenses.normalized_status
  is 'PassTo canonical status. Eight values. Pending = source has pending application/renewal/review state; credential issuance depends on mapping rules.';
```

Verification confirmed the live constraint includes `Pending`.

---

## TASK-0007 Decisions Compliance Checklist

| Decision | Requirement | Result |
|---|---|---|
| OD-1 | `audit_events.action` CHECK: `resource.verb` regex | Passed |
| OD-2 | `licenses.is_primary`: partial unique index, service-role only | Passed |
| OD-3 | SQL regression tests + Supabase advisors + manual smoke tests | Covered by TASK-0015 and follow-up verification |
| OD-4 | `show_qr` form-gated; creates `verifiers` record | Schema supports; launch implementation deferred and guarded |
| OD-5 | `verification_tokens.token_type`: no `pdf_qr` | Passed |
| OD-6 | `passes` → `credentials` | Passed |
| OD-7 | `stripe_subscriptions` → `subscriptions`; `stripe_customers` deviation accepted | Passed with ADR-0001 |
| OD-8 | `purchases` → `payments` | Passed |
| OD-9 | `communication_events` → `notification_events` | Passed |
| OD-10 | `stripe_events` idempotency table | Passed |
| OD-11 | Nurse `profiles` UPDATE: SECURITY DEFINER RPC only | Passed |
| OD-12 | `license_status_mappings` reference table | Passed; 42 rows across 8 groups |

---

## Current Gate Status

- Phase 2 profile/auth/onboarding work is unblocked.
- Phase 3.3 license lookup is no longer blocked by the `Pending` status mismatch; R4 resolved it.
- Phase 3.5 selfie upload has its Storage foundation in place from TASK-0020; Lovable UI and backend confirmation remain future implementation work.
- Show QR remains deferred. No task may create `show_qr` tokens until David reopens that scope.

---

## Confirmation

No migration was applied as part of the original TASK-0018 review. Later remediation work was applied through separate approved execution steps and documented separately.

---

## David Approval

Pending final archive/closeout confirmation — 2026-05-27.
