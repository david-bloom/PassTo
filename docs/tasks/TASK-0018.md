# TASK-0018 — Implement Account Profile Foundation and Onboarding Routing

**Task ID:** TASK-0018
**Title:** Implement Account Profile Foundation and Onboarding Routing
**Owner:** Claude
**Status:** Blocked — Awaiting Repo-Visible Implementation Handoff
**Priority:** Critical
**Created Date:** 2026-05-28
**Approved Date:** Pending in this task file
**PRD Section:** Section 3 — End-to-End MVP User Journeys
**Sequence:** 1 of 13
**Depends On:** TASK-0016, TASK-0017
**Blocks:** TASK-0019

## Product Goal

Make the beginning of the MVP nurse journey work against the canonical Supabase project: account creation, profile initialization, login/logout/password flows, and routing into the launch-critical onboarding sequence.

## Technical Scope

- Confirm P1/P2/P3 auth screens use the canonical PassTo Supabase client.
- Implement or verify profile creation/init behavior for new Supabase Auth users.
- Ensure onboarding routing moves the nurse from account creation into identity verification.
- Ensure Lovable uses only publishable key plus user JWT.
- Write account/profile audit events where supported by the current schema and backend surface.
- Remove or ignore old Airtable/Make identity assumptions in this journey.

## Out of Scope

- ID.me exchange implementation.
- Twilio verification.
- License lookup, data match, selfie, credential, wallet, Stripe, or verifier implementation.
- Production launch approval.

## Acceptance Criteria

- [ ] New nurse signup creates/authenticates a Supabase user in `wvzjfxacykgsaffskgtr`.
- [ ] A matching `profiles` row exists or is initialized for the authenticated nurse.
- [ ] Login, logout, reset password, and update password paths still function.
- [ ] Account creation routes to the next onboarding step instead of creating a credential or license prematurely.
- [ ] No service-role secrets are present in Lovable code.
- [ ] Claude documents files changed, tests run, deviations, and risks.

## QA Plan

- Manual smoke test signup/login/logout/reset/update-password paths.
- Verify profile row creation in Supabase.
- Confirm browser console/network has no Supabase project mismatch.
- Confirm account creation does not create license, credential, wallet, or payment records.

## David Approval Request

David approval is required before Claude executes this Class A implementation task.

## Codex Handoff Note — 2026-05-28

### Claude Report Received From David

David reported that Claude responded:

```text
This remote execution environment cannot push to david-bloom/PassTo — both git push and GitHub MCP writes return 403. The commit is staged locally on branch claude/awesome-hamilton-SdGBx.
```

### Current Repo-Visible State

At the time of this Codex update, Claude's implementation commit is not visible in GitHub. Codex cannot review, approve, or route TASK-0018 based only on a chat-only report or a local-only staged commit in Claude's execution environment.

This task is therefore blocked until Claude's implementation is made repo-visible.

### Action Required From Claude

When David gives Claude the `C` prompt, Claude should not continue implementing new scope. Claude should first produce a repo-visible handoff for the staged TASK-0018 work.

Because Claude reports GitHub write/push permission failures, acceptable handoff options are:

1. Fix GitHub permissions and push the branch `claude/awesome-hamilton-SdGBx`, then open or link a PR for TASK-0018.
2. If push remains blocked, provide a complete patch artifact from the staged commit using `git diff --staged` plus an implementation summary, test results, changed files, deviations, and risks. David or Codex can then apply the patch through an environment with GitHub write access.
3. If the work was committed but not pushed, provide both `git status --short` and `git show --stat --patch HEAD` for the TASK-0018 commit.

Claude should include:

- Task ID: TASK-0018
- Branch: `claude/awesome-hamilton-SdGBx`
- Commit SHA if one exists
- Files changed
- Implementation summary
- Tests run and results
- Deviations from approved scope, if any
- Risks/issues, if any
- Whether David approval for execution was recorded, and where

### Codex Review Position

Codex review is pending. Codex cannot compare Claude's claims against implementation artifacts until the staged work is visible as a PR, branch, patch, or commit diff.

### Next Owner

Claude owns the next action: make the TASK-0018 staged implementation repo-visible or provide a complete patch/diff handoff.
