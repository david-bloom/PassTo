# TASK-0007 — Codex QA Re-Review of Supabase Schema and RLS Plan

**Task ID:** TASK-0007  
**Title:** Codex QA Re-Review of Supabase Schema and RLS Plan  
**Owner:** Codex  
**Status:** Done  
**Priority:** High  
**Created Date:** 2026-05-26  
**Approved Date:** 2026-05-26  
**Completed Date:** 2026-05-26  
**Approval Source:** David approval 2026-05-26  
**Related Tasks:** TASK-0006, Claude Task 001-R  
**Codex Response:** `/docs/architecture/CODEX_RESPONSE_TASK_0007_SCHEMA_QA.md`  

---

## Product Goal

Codex re-reviews the Supabase Schema and RLS Plan and resolves all 12 open decisions so that a final migration SQL (v4) can be written and submitted for David migration approval.

---

## Technical Scope

Review and act on:

```text
/docs/architecture/SUPABASE_SCHEMA_RLS_PLAN.md
```

Using these source documents:

```text
/docs/tasks/2026-05-24-claude-task-001-supabase-schema-remediation.md  — v3 SQL source
/docs/tasks/2026-05-24-claude-task-001-codex-qa-review.md              — prior QA findings
/docs/activity_log/DECISION-0011-MVP-MIGRATION-BLOCKING-DECISIONS.md   — approved decisions
/docs/architecture/NAMING_CONVENTIONS.md                               — canonical naming
/docs/architecture/LOVABLE_SUPABASE_VERCEL_RESPONSIBILITY_MAP.md       — platform ownership
/docs/architecture/DATA_MODEL.md
/docs/architecture/SECURITY_MODEL.md
```

---

## Codex Must Resolve All 12 Open Decisions

### From v3 Remediation

| # | Decision | Area | Resolution |
|---|---|---|---|
| OD-1 | Define `audit_events.action` canonical namespace | Schema | Resolved — use `resource.verb` with regex shape constraint |
| OD-2 | Confirm `is_primary` designation implementation path | Schema / RLS | Resolved — service-role backend route/function only |
| OD-3 | Confirm RLS testing approach before migration | QA process | Resolved — SQL RLS tests + Supabase advisors + manual smoke tests |
| OD-4 | Confirm `verifiers` record behavior for `show_qr` tokens | Schema / Product | Resolved — `show_qr` is form-gated and creates verifier/event records |
| OD-5 | Confirm whether PDF QR token type (`pdf_qr`) is in MVP scope | Schema / Product | Resolved — `pdf_qr` out of MVP migration scope |

### From Naming Reconciliation (TASK-0006)

| # | Decision | Resolution |
|---|---|---|
| OD-6 | `passes` → `credentials` rename | Resolved — accept rename |
| OD-7 | `stripe_subscriptions` → `subscriptions` rename | Resolved — accept rename |
| OD-8 | `purchases` → `payments` rename | Resolved — accept rename |
| OD-9 | `communication_events` → `notification_events` rename | Resolved — accept rename |
| OD-10 | Add `stripe_events` idempotency table | Resolved — add table |
| OD-11 | Scope nurse UPDATE policy on `profiles` to safe columns only | Resolved — nurse may update only approved self-service fields |
| OD-12 | Decide whether `license_status_mappings` is a DB reference table or Edge Function logic | Resolved — add MVP reference table |

---

## Acceptance Criteria

- [x] All 12 open decisions resolved or escalated to David with a clear recommendation
- [x] Final naming decisions confirmed for all 5 renamed/added tables
- [x] `audit_events.action` canonical namespace defined
- [x] `profiles` nurse UPDATE column scope defined
- [x] `stripe_events` table design confirmed or rejected with rationale
- [x] RLS testing approach confirmed
- [x] show_qr verifier form behavior confirmed
- [x] PDF QR token type decision confirmed (in or out of MVP)
- [x] Codex produces a written QA response document or updates the schema plan with decisions
- [x] If all decisions resolved: Codex authorizes Claude to write v4 migration SQL

---

## Out of Scope

- Writing the v4 migration SQL (Claude's job after Codex QA)
- Applying any migration to Supabase
- David migration approval (separate task after v4 SQL)

---

## Codex Completion Summary

Codex produced:

```text
/docs/architecture/CODEX_RESPONSE_TASK_0007_SCHEMA_QA.md
```

The response resolves all 12 open decisions and authorizes Claude to draft v4 migration SQL.

Important correction identified by Codex:

```text
Old schema-plan project ref: zpvbexzdiklxlvrxsvop
Current connected PassTo Supabase project ref: wvzjfxacykgsaffskgtr
```

Claude must target `wvzjfxacykgsaffskgtr` for v4 migration notes unless David explicitly identifies a different dev/prod project.

---

## Migration Status

```text
v4 migration SQL: Authorized for drafting
migration execution: Not approved
David migration approval: still required
```

---

## Recommended Next Task

```text
TASK-0011 — Claude Draft v4 Supabase Migration SQL From TASK-0007 Decisions
```

Claude should incorporate the Codex response, update the project ref, and produce migration SQL plus an RLS test plan. Claude must not apply the migration.

---

## Risks / Issues

- `pdf_qr` is intentionally excluded from MVP migration scope. Adding QR behavior inside PDFs later will require a separate approved task and migration.
- `show_qr` is now confirmed as form-gated. If David later wants instant scan-without-form behavior, schema and event logic will need revision.
- `license_status_mappings` is now included in MVP scope as a reference table; seed data still needs to be defined.
- Migration execution remains blocked until David explicit approval.

---

## David Done Decision

Pending David final Done confirmation.
