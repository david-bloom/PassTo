# TASK-0053 — Codex QA Phase 4 Credential and Wallet Issuance

**Task ID:** TASK-0053  
**Title:** Codex QA Phase 4 Credential and Wallet Issuance  
**Owner:** Codex  
**Status:** Codex QA Complete — Ready for David Review  
**Priority:** Critical  
**Created Date:** 2026-06-02  
**Approved Date:** 2026-06-02  
**PRD Phase:** Phase 4 — Credential and Wallet Issuance  
**Phase Item:** 4.5  
**PRD Sections:** Section 3, Section 4, Section 5, Section 6  
**Depends On:** TASK-0049, TASK-0050, TASK-0051, TASK-0052  
**Blocks:** Phase 5 dashboard/share-link work

## Product Goal

Verify that Phase 4 safely creates credentials, issues or tracks wallet passes, records durable provider state, and exposes honest success/status UX before downstream dashboard and verifier work depends on it.

## Technical Scope

- Review GitHub task docs and implementation notes.
- Review deployed Supabase Edge Functions and/or Vercel wallet routes.
- Verify credential creation gate behavior.
- Verify wallet signing contract and provider-state persistence.
- Verify `/success` / PassReady status behavior.
- Verify audit/event and ops visibility records.
- Verify RLS/direct-write boundaries.
- Record pass/block findings in GitHub task docs, activity log, and relevant issue/comment thread.

## Out of Scope

- Implementing fixes.
- Approving production launch.
- Approving live wallet provider/certificate changes.
- Phase 5 dashboard/share-link QA.
- Stripe QA beyond confirming Phase 4 uses server-confirmed entitlement state.

## Routes Affected

- `/success`
- `/pass-ready` if retained

## Components Affected

- Credential creation function/route.
- Wallet signing route/function.
- Wallet provider state writer.
- Success/PassReady status endpoint and UI.

## Database/Tables Affected

- `profiles`
- `licenses`
- `credentials`
- `wallet_passes`
- `subscriptions`
- `audit_events`
- `notification_events`

## Integrations Affected

- Supabase
- Vercel if used for wallet signing
- Apple Wallet / PassKit
- Google Wallet

## Security/RLS Impact

Security Impact: High

Codex must explicitly test or review that client-side writes cannot create credentials, alter wallet provider truth, or bypass issuance gates.

## Design System Impact

Design System Impact: Medium  
Design Source: `/docs/design_system/README.md`

Codex should review whether `/success` status states are clear, non-misleading, and consistent with the product/status UI.

## Acceptance Criteria

- [ ] Credential creation refuses every missing/failed trust gate.
- [ ] Successful credential creation writes correct canonical state.
- [ ] Wallet signing contract keeps secrets backend-only.
- [ ] Wallet provider state is durable in `wallet_passes`.
- [ ] Wallet failure/pending states are visible and do not misrepresent status.
- [ ] `/success` direct navigation cannot skip gates.
- [ ] Audit/event records exist for success and failure.
- [ ] RLS/direct client access cannot mutate credential/wallet trust state.
- [ ] Codex records findings and next required action in GitHub.

## QA Plan

- Source review of implementation files and task docs.
- Live deployment/version check for relevant Edge Functions/routes.
- SQL/RLS review for credential and wallet tables.
- Manual or simulated test cases for all-gates-pass, missing gate, provider failure, pending wallet, and retry.
- User-facing review of `/success` states if Lovable UI is available.

## David Approval Request

David approved Phase 4 execution on 2026-06-02. TASK-0053 is the Phase 4 QA closure task.

## Phase 4 Individual Task QA Status

| Task | QA Status | Live |
|---|---|---|
| TASK-0049 `credential-create` | ✅ Codex QA Complete — Pass with deferrals (Re-QA v2) | v2, Migration L applied |
| TASK-0050 `wallet-issue` + contract | ✅ Codex QA Complete — Pass with deferrals | v3 |
| TASK-0051 `success-status` update | ✅ Codex QA Complete — Pass with deferrals | v7 (doc previously said v8 — label drift, not functional) |
| TASK-0052 `/success` Lovable page | ✅ Codex QA Complete — Pass with deferrals | Live tested |

## Umbrella QA Verdict — Pass with Deferrals (2026-06-02)

**Proposed by:** Codex  
**Accepted by:** Claude (Main Conductor) — 2026-06-02  
**Verdict:** Pass with deferrals — Phase 4 ready for David Done review

All individual task P1/P2 findings have been remediated. No blockers remain.

### Non-Blocking Risks / Deferrals

| Item | Type |
|---|---|
| `success-status` live version is v7; docs previously referenced v8 — source matches remediation, label drift only | Doc drift — non-blocking |
| Migration L not visible in migration-list view; schema inspection confirms `credentials_profile_license_unique` is live | Tooling view gap — non-blocking |
| Real Apple/Google wallet issuance untested until certificates, issuer, Vercel env vars, and signing secrets are configured | Pre-production deferral — hard gate |
| Vercel production deployment of `api/sign-apple.js` / `api/sign-google.js` not live-verified | Pre-production deferral — hard gate |
| `credentials` and `wallet_passes` have broad direct grants to `anon`/`authenticated`; RLS-backed SELECT only — not a current bypass | Recommended cleanup before production |

### Recommended Pre-Production Cleanup (not blocking Done)

1. Correct doc references from `success-status v8` to `v7`, or redeploy with aligned version label.
2. Consider revoking broad direct table grants from `anon`/`authenticated` for `credentials` and `wallet_passes`, leaving RLS-backed SELECT only.

## QA Finding Summary — All Tasks

### TASK-0049 Findings

| Finding | Severity | Status |
|---|---|---|
| No DB unique constraint on `credentials(profile_id, license_id)` — function-only idempotency | P2 | ✅ Remediated — Migration L applied, `credential-create` handles `23505` |

### TASK-0050 Findings

| Finding | Severity | Status |
|---|---|---|
| `@supabase/supabase-js` missing from `package.json` | P1 | ✅ Remediated |
| Asset files had macOS " copy" suffix — `require()` would fail | P1 | ✅ Remediated |
| `wallet_passes` pending upsert not checked — could return 200 with no DB state | P1 | ✅ Remediated — 500 on init failure |
| Post-provider `wallet_passes` update not checked — could return `issued` with no DB record | P1 | ✅ Remediated — fail-closed, marks `error` on persistence failure |
| Credential activation not checked — could return `active` when DB not updated | P1 | ✅ Remediated — returns `wallet_activation_partial: true` partial state |
| Response reflected provider success, not DB state | P2 | ✅ Remediated — response uses `applePersisted`/`googlePersisted` flags |
| Real Apple/Google signing not testable | Deferral | ⬜ Pending certificate/issuer procurement |

### TASK-0051 Findings

| Finding | Severity | Status |
|---|---|---|
| `success-status` reads not fail-closed — DB errors silently returned as `none`/`false` | P2 | ✅ Remediated — returns 503 on read error |
| Legacy `wallet_pass_url` not status-gated | P2 | ✅ Remediated — gated on `status = issued` |
| Ops wallet failure notification deferred | Deferral | ⬜ No email/SMS send infra yet |

### TASK-0052 Findings

| Finding | Severity | Status |
|---|---|---|
| No P1/P2 blockers | — | — |
| Real wallet signing not testable | Deferral | ⬜ Same as TASK-0050 |

## Remaining Approval Boundaries

David approval still required before:
- Done decisions on any Phase 4 task
- Production launch
- Apple Wallet certificate/private key handling
- Google Wallet issuer setup and service account key
- Vercel production deployment of signing routes
- Wallet issuance launch
- Credential issuance launch
- Phase 5 production-dependent work
- Risk acceptance
