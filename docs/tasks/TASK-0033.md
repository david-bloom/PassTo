# TASK-0033 — Implement Twilio Phone Verification Journey

**Task ID:** TASK-0033
**Title:** Implement Twilio Phone Verification Journey
**Owner:** Claude
**Status:** Spec Drafted — Awaiting David Approval
**Priority:** Critical
**Created Date:** 2026-05-28
**Approved Date:** Pending
**PRD Section:** Section 3 — End-to-End MVP User Journeys
**Sequence:** 3 of 13
**Depends On:** TASK-0031, TASK-0032
**Blocks:** TASK-0034

## Product Goal

Add the launch-critical phone possession gate so credential issuance remains blocked until the nurse completes Twilio SMS verification.

## Technical Scope

- Implement backend send-code and verify-code operations using Twilio.
- Wire Lovable phone entry and code entry UI to the backend operations.
- Persist phone verification state on the nurse profile or approved verification state table.
- Write audit/event records for send, verify, failure, retry, and lockout behavior where supported.
- Provide clear retry/failure routing.

## Out of Scope

- Twilio A2P 10DLC production approval.
- License lookup, data matching, selfie, credential, wallet, or Stripe implementation.
- Production launch fallback approval.

## Acceptance Criteria

- [ ] Authenticated nurse can request an SMS verification code.
- [ ] Authenticated nurse can submit the code for verification.
- [ ] Phone verification success advances to license lookup.
- [ ] Failed/expired/too-many-attempts states do not advance the journey.
- [ ] Credential issuance remains blocked until phone verification passes.
- [ ] Claude documents tests, files changed, deviations, and risks.

## QA Plan

- Test send-code success and provider failure behavior.
- Test correct code, incorrect code, expired code, and retry path.
- Verify no Twilio secrets are exposed to Lovable.

## David Approval Request

David approval is required before Claude executes this Class A implementation task.

---

## Execution Results — 2026-05-30

**Approved by David:** Yes — verbally authorized execution 2026-05-30.

**David Approval override note:** TASK-0026 spec stated this task should not begin until TASK-0025 Phase 3.1 QA passes. David approved execution prior to Phase 3.1 completion. Phase 3.2 Edge Functions and Lovable UI are built and deployed; end-to-end QA with a real IAL2-verified user is deferred until Phase 3.1 (ID.me sandbox run) completes. Manual test path available via direct profile state update in Supabase.

### Supabase Edge Functions Deployed

| Function | Version | Status | Deployed |
|---|---|---|---|
| `phone-send-otp` | v1 | ACTIVE | 2026-05-30 |
| `phone-verify-otp` | v1 | ACTIVE | 2026-05-30 |

### Secrets Set in Supabase (wvzjfxacykgsaffskgtr)

- `TWILIO_ACCOUNT_SID` — set
- `TWILIO_AUTH_TOKEN` — set
- `TWILIO_VERIFY_SERVICE_SID` — set

### Lovable P2 Changes

- `src/pages/VerifyPhone.tsx` created — two-step phone + OTP flow
- `/verify-phone` route registered in `App.tsx`
- Uses `@/integrations/passto-supabase/client`

### Deviations

| # | Deviation | Reason |
|---|---|---|
| D-1 | Executed before TASK-0025 Phase 3.1 QA complete | David approved; end-to-end QA deferred to Phase 3.1 completion |
| D-2 | CORS restricted to `enroll.passtodigital.com` | Tighter than `*` used in `idme-exchange`; aligns with production security |

### Status

**In Progress — Pending Codex QA and Phase 3.1 end-to-end test.**

Pre-Phase-3.1 manual QA path: update a test user's profile to `onboarding_step = 'phone'`, `id_verification_status = 'verified'`, `id_verification_level = 'IAL2'` via SQL, then run the `/verify-phone` flow.

### Needs Codex QA

Yes — see TASK-0026 for full spec. Codex should review `phone-send-otp` and `phone-verify-otp` against the TASK-0026 spec before Phase 3.2 QA is run.
