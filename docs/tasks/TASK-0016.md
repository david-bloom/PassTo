# TASK-0016 — Update P1, P2, P3 Lovable Projects to Canonical Supabase

**Task ID:** TASK-0016
**Title:** Update P1, P2, P3 Lovable Projects to Canonical Supabase (`wvzjfxacykgsaffskgtr`)
**Owner:** David (Lovable editor) + Claude (spec and publishable key retrieval)
**Status:** Done — 2026-05-26
**Priority:** Critical
**Created Date:** 2026-05-26
**Blocked By:** TASK-0015 (schema must be verified before live traffic switches)
**Blocks:** TASK-0017 (dead code removal)
**Related Tasks:** TASK-0013, TASK-0014, TASK-0015, TASK-0017

---

## Product Goal

Point all three Lovable projects at the single canonical Supabase instance (`wvzjfxacykgsaffskgtr`). This is the moment all live traffic moves to the verified v4 schema. All data and auth operations from this point forward write to the canonical database.

---

## Execution Notes

Lovable's ENV vars (`VITE_SUPABASE_URL`, `VITE_SUPABASE_PROJECT_ID`) are auto-generated and locked to the Lovable Cloud managed Supabase — they cannot be changed via the Lovable editor. The correct pattern in all three projects is a dual-client setup:

- `src/integrations/supabase/client.ts` — Lovable Cloud managed, locked, untouched
- `src/integrations/passto-supabase/client.ts` — PassTo canonical (`wvzjfxacykgsaffskgtr`), hardcoded URL + anon key

All PassTo app files were migrated to import from `passto-supabase/client` instead of the Lovable Cloud client. Lovable AI executed the import switches in each project.

**Discovery during execution:** P2 `CreateAccount.tsx` had a residual import from the Lovable Cloud client for the `create-airtable-record` dead function call. Fixed by removing the call entirely (dead Airtable code — partial TASK-0017 pre-work). Confirmed clean via network tab.

---

## Canonical Supabase Credentials Used

```
URL:      https://wvzjfxacykgsaffskgtr.supabase.co
ANON KEY: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Ind2empmeGFjeWtnc2FmZnNrZ3RyIiwicm9sZSI6ImFub24iLCJpYXQiOjE3Nzk4MDMxMTAsImV4cCI6MjA5NTM3OTExMH0.aKHSehfonHiIoXkEBkus399ODd-62tuGcGnKfJ8FceU
```

---

## Files Updated Per Project

### P1 — PassTo Website (`passtodigital.com`)
12 files switched + `passto-supabase/client.ts` created + `passto-supabase/types.ts` created:
`AccessGateModal.tsx`, `ShareModal.tsx`, `CreateAccount.tsx`, `IDVerification.tsx`, `Login.tsx`, `MyAccount.tsx`, `PassReady.tsx`, `Payment.tsx`, `ResetPassword.tsx`, `Signup.tsx`, `UpdatePassword.tsx`, `V2VerifyDemo.tsx`

### P2 — PassTo Enroll (`enroll.passtodigital.com`)
3 files switched + `passto-supabase/client.ts` updated + `passto-supabase/types.ts` created + `create-airtable-record` call removed from `CreateAccount.tsx`:
`CreateAccount.tsx`, `UploadSelfie.tsx`, `SecondLicense.tsx`

### P3 — PassTo App (`app.passtodigital.com`)
5 files switched + `passto-supabase/client.ts` created + `passto-supabase/types.ts` created:
`Login.tsx`, `MyAccount.tsx`, `ResetPassword.tsx`, `UpdatePassword.tsx`, `ShareModal.tsx`

---

## Acceptance Criteria

- [x] Claude retrieved and provided the canonical anon/publishable key
- [x] P1 all app files switched to `passto-supabase/client` (canonical `wvzjfxacykgsaffskgtr`)
- [x] P2 all app files switched to `passto-supabase/client` (canonical `wvzjfxacykgsaffskgtr`)
- [x] P3 all app files switched to `passto-supabase/client` (canonical `wvzjfxacykgsaffskgtr`)
- [x] P2 dead `create-airtable-record` call removed from `CreateAccount.tsx`
- [x] P1 smoke test passed (`passtodigital.com`)
- [x] P2 smoke test passed (`enroll.passtodigital.com`)
- [x] P3 smoke test passed (`app.passtodigital.com`)
- [x] No Supabase connection errors in any project

---

## David Approval

Done — 2026-05-26.

## Recommended Next Task

TASK-0017 — Remove dead Airtable code from P1, P2, P3.
