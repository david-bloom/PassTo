# TASK-0016 — Update P1, P2, P3 Lovable Projects to Canonical Supabase

**Task ID:** TASK-0016
**Title:** Update P1, P2, P3 Lovable Projects to Canonical Supabase (`wvzjfxacykgsaffskgtr`)
**Owner:** David (Lovable editor) + Claude (spec and publishable key retrieval)
**Status:** Not Started
**Priority:** Critical
**Created Date:** 2026-05-26
**Blocked By:** TASK-0015 (schema must be verified before live traffic switches)
**Blocks:** TASK-0017 (dead code removal)
**Related Tasks:** TASK-0013, TASK-0014, TASK-0015, TASK-0017

---

## Product Goal

Point all three Lovable projects at the single canonical Supabase instance (`wvzjfxacykgsaffskgtr`). This is the moment all live traffic moves to the verified v4 schema. All data and auth operations from this point forward write to the canonical database.

---

## Technical Scope

### Step 1 — Claude Retrieves Canonical Supabase Publishable Key

Before David makes changes, Claude fetches the anon/publishable key for `wvzjfxacykgsaffskgtr` using the Supabase MCP `get_publishable_keys` tool, so David has the correct value ready to paste.

### Step 2 — David Updates ENV Vars in All 3 Lovable Projects

Each project update is done via Lovable editor: **Project Settings → Environment Variables**.

---

## ENV Var Changes Per Project

### P1 — PassTo Website (`6c973fd1-2dcd-4377-8c98-4d2f0d68732e`)

| Variable | Action | New Value |
|---|---|---|
| `VITE_SUPABASE_URL` | **Change** | `https://wvzjfxacykgsaffskgtr.supabase.co` |
| `VITE_SUPABASE_PUBLISHABLE_KEY` | **Change** | `<anon key for wvzjfxacykgsaffskgtr>` |
| `VITE_SUPABASE_PROJECT_ID` | **Change** | `wvzjfxacykgsaffskgtr` |

### P2 — PassTo Enroll (`d279ccd3-8397-4e7b-933c-8f5c8468d19e`)

| Variable | Action | New Value |
|---|---|---|
| `VITE_SUPABASE_URL` | **Change** | `https://wvzjfxacykgsaffskgtr.supabase.co` |
| `VITE_SUPABASE_PUBLISHABLE_KEY` | **Change** | `<anon key for wvzjfxacykgsaffskgtr>` |
| `VITE_WEBHOOK_IDME_CALLBACK` | **Remove** | (Make webhook — no longer needed) |
| `WEBHOOK_IDME_CALLBACK` | **Remove** | (duplicate in .env files) |
| `VITE_IDME_CLIENT_ID` | **Add** | `<production ID.me client_id when available — leave sandbox value for now>` |

### P3 — PassTo App (`9a223cc4-ef58-43d4-929a-4c0424b586c2`)

| Variable | Action | New Value |
|---|---|---|
| `VITE_SUPABASE_URL` | **Change** | `https://wvzjfxacykgsaffskgtr.supabase.co` |
| `VITE_SUPABASE_PUBLISHABLE_KEY` | **Change** | `<anon key for wvzjfxacykgsaffskgtr>` |

---

## Smoke Test After Each Project Switch

After updating each project, run a quick smoke test before switching the next:

**P1 smoke test:** Visit `passtodigital.com` → page loads → no Supabase connection errors in browser console
**P2 smoke test:** Visit `enroll.passtodigital.com` → `/create-account` loads → no Supabase connection errors
**P3 smoke test:** Visit P3 URL (TBD) → login screen loads → no Supabase connection errors

If any project shows connection errors after the switch: revert that project's env vars and report to Claude before switching the next.

---

## Important Notes

1. **Auth sessions from old Lovable Supabase instances are invalidated** when the URL changes. Any nurse currently logged in will need to re-authenticate. At this stage (pre-launch), there are no real users — this is acceptable.

2. **P2 Make webhook removal:** Removing `VITE_WEBHOOK_IDME_CALLBACK` at this step means the ID.me callback will break until `idme-exchange` Edge Function is deployed (TASK-0012 implementation). Coordinate timing with David — this switch should happen after the edge functions are deployed, or accept that the enrollment flow is temporarily broken.

3. **Order recommendation:** Switch P1 first (lowest risk — marketing only). Then P3. Then P2 last (most complex, depends on edge functions).

---

## Acceptance Criteria

- [ ] Claude has retrieved and provided the canonical anon/publishable key
- [ ] P1 `VITE_SUPABASE_URL` updated to `wvzjfxacykgsaffskgtr`
- [ ] P2 `VITE_SUPABASE_URL` updated to `wvzjfxacykgsaffskgtr`
- [ ] P3 `VITE_SUPABASE_URL` updated to `wvzjfxacykgsaffskgtr`
- [ ] P2 Make webhook ENV vars removed
- [ ] P1, P2, P3 smoke tests passed
- [ ] No Supabase connection errors in any project

---

## David Approval

Not started.

## Recommended Next Task

TASK-0017 — Remove dead Airtable code from P1, P2, P3.
