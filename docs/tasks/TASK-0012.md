# TASK-0012 — Write v4 Migration SQL for `wvzjfxacykgsaffskgtr`

**Task ID:** TASK-0012
**Title:** Write v4 Migration SQL for `wvzjfxacykgsaffskgtr`
**Owner:** Claude
**Status:** Not Started
**Priority:** Critical
**Created Date:** 2026-05-26
**Blocked By:** None — all ODs resolved (FD-029)
**Blocks:** TASK-0013 (David review)
**Related Tasks:** TASK-0006, TASK-0007, TASK-0011

---

## Authorization

Claude is authorized to write the v4 migration SQL per `CODEX_RESPONSE_TASK_0007_SCHEMA_QA.md`:
> "Claude is authorized to draft v4 migration SQL, but the migration must not be applied until David gives explicit migration approval."

The migration must target `wvzjfxacykgsaffskgtr` (PassTo Dev) exclusively.

---

## Product Goal

Produce a complete, reviewable v4 migration SQL file that creates the full PassTo MVP schema in the canonical Supabase instance. This is the database foundation for every implementation task that follows.

---

## Technical Scope

**Source documents:**
- `docs/architecture/SUPABASE_SCHEMA_RLS_PLAN.md` — base schema and RLS model
- `docs/architecture/CODEX_RESPONSE_TASK_0007_SCHEMA_QA.md` — all 12 OD resolutions
- `docs/decisions/DECISIONS_LOG.md` — FD-001 through FD-029 (all resolved)
- `docs/tasks/2026-05-24-claude-task-001-supabase-schema-remediation.md` — v3 SQL source

**Deliverable:**
```
docs/architecture/V4_MIGRATION_SQL.md
```

The SQL is delivered as a Markdown file for review. It will be executed via Supabase MCP `apply_migration` in TASK-0014 after David approval.

---

## Required Tables (15 total)

| # | Table | Notes |
|---|---|---|
| 1 | `profiles` | Nurse identity; linked to `auth.users` |
| 2 | `licenses` | Nurse license records; `is_primary` service-role only |
| 3 | `credentials` | Renamed from `passes`; owns credential state |
| 4 | `wallet_passes` | Apple + Google pass records |
| 5 | `verification_tokens` | `share_link` and `show_qr` types only (no `pdf_qr`) |
| 6 | `verifiers` | Form-gated verifier capture; both token types |
| 7 | `verification_events` | Token usage events |
| 8 | `subscriptions` | Renamed from `stripe_subscriptions` |
| 9 | `payments` | Renamed from `purchases` |
| 10 | `stripe_events` | Webhook idempotency table (new — OD-10) |
| 11 | `notification_events` | Renamed from `communication_events` |
| 12 | `pdf_exports` | PDF generation records |
| 13 | `audit_events` | `resource.verb` action namespace; append-only |
| 14 | `license_lookups` | License lookup attempt log |
| 15 | `license_status_mappings` | DB reference table (FD-024); includes initial data |

---

## Required SQL Sections

1. **Extensions** — `uuid-ossp`, `pgcrypto` (if not already enabled)
2. **Tables** — All 15 tables with correct column types, constraints, FKs, defaults
3. **RLS** — `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` for all tables; policies per SUPABASE_SCHEMA_RLS_PLAN.md
4. **Indexes** — Performance indexes on FK columns and frequently-queried fields
5. **Initial data** — `license_status_mappings` seed rows
6. **Nurse UPDATE policy** — Scoped to `first_name`, `last_name`, `phone`, `avatar_url` only (OD-11)
7. **audit_events** — `resource.verb` CHECK constraint; no DELETE policy (append-only)
8. **stripe_events** — Idempotency table with `stripe_event_id` unique constraint

---

## Out of Scope

- `pdf_qr` token type (OD-5 — out of MVP)
- Applying the SQL (TASK-0014)
- RLS regression tests (TASK-0015)
- Data from old Lovable-managed Supabase instances

---

## Acceptance Criteria

- [ ] All 15 tables present with correct column definitions
- [ ] All FK relationships correctly defined
- [ ] RLS enabled and policy SQL written for all tables
- [ ] Nurse UPDATE policy on `profiles` scoped to safe fields only
- [ ] `audit_events.action` CHECK constraint enforces `resource.verb` shape
- [ ] `verification_tokens.token_type` constraint includes only `share_link` and `show_qr`
- [ ] `stripe_events` table with unique `stripe_event_id` constraint
- [ ] `license_status_mappings` seed data included
- [ ] SQL is valid PostgreSQL (no syntax errors)
- [ ] Migration targets `wvzjfxacykgsaffskgtr` — noted in file header
- [ ] Ready for David review (TASK-0013)

---

## David Approval

Not started.

## David Done Decision

Pending.

## Recommended Next Task

TASK-0013 — David reviews and approves v4 migration SQL before it is applied.
