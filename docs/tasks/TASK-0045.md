# TASK-0045 — Implement ID.me-First Onboarding Attempt and Account/Profile Linking Backend

**Task ID:** TASK-0045  
**Title:** Implement ID.me-First Onboarding Attempt and Account/Profile Linking Backend  
**Owner:** Claude  
**Status:** Codex Re-QA Approved — Downstream Backend May Proceed; P2 Production Hardening Open  
**Priority:** Critical  
**Created Date:** 2026-05-31  
**Approved Date:** 2026-06-01  
**PRD Section:** Section 3 — End-to-End MVP User Journeys  
**Supports:** `/docs/flows/IDME_FIRST_ONBOARDING.md`  
**Depends On:** TASK-0025 completion, canonical Supabase project confirmation  
**Blocks:** TASK-0046, TASK-0047, Lovable ID.me-first UX implementation  

## Product Goal

Allow nurse onboarding to begin with ID.me verification instead of password-first account creation, while preserving backend trust boundaries, safe account/profile linking, and auditability.

## Technical Scope

- Update the ID.me backend workflow so `/id-verification` can start before a nurse has completed traditional account creation.
- Create or reuse a safe pre-account onboarding state, such as `onboarding_attempts`, if the current schema cannot safely represent this flow.
- Validate ID.me state and PKCE server-side.
- Exchange ID.me authorization code server-side only.
- Store verified identity result and safe contact fields needed for `/confirm-info`.
- Support profile/account creation or linking after ID.me verification and contact confirmation.
- Enforce one active profile/account linkage per ID.me subject unless Codex/David approve an exception.
- Record audit events for ID.me start, success, failure, expired/abandoned attempt, and account/profile link.
- Preserve the existing service-role boundary. Lovable must not receive secrets or make trust decisions.

## Out of Scope

- License lookup implementation.
- Twilio phone verification implementation.
- Stripe/payment implementation.
- Selfie upload implementation.
- Wallet pass issuance.
- Production ID.me environment changes unless separately approved.

## Routes Affected

- `/id-verification`
- `/auth/idme/callback`
- `/confirm-info`

## Components Affected

- Lovable ID.me start page and callback caller.
- Backend ID.me Edge Function or equivalent trusted route.
- Onboarding routing helper, if one exists.

## Database/Tables Affected

- `profiles`
- `audit_events`
- Possible new `onboarding_attempts` or equivalent temporary state table.

Claude must not apply a migration without David approval. If a schema change is required, Claude must provide SQL and wait for the required approval/application path.

## Integrations Affected

- ID.me Sandbox / production later
- Supabase Auth
- Supabase Edge Functions

## Security/RLS Impact

Security Impact: High

This task changes the authentication and identity-linking order. Codex security review is required before execution and again before production use.

Key risks:

- Account takeover if ID.me state/account linking is weak.
- Duplicate profiles linked to one ID.me subject.
- Email enumeration during account linking.
- Client-side exposure of identity fields that should remain server-side.

## Design System Impact

Design System Impact: Medium  
Design Source: `/docs/design_system/README.md`

The frontend must follow PassTo design standards, but this backend task does not implement final UI.

## Acceptance Criteria

- [ ] Nurse can start ID.me before password-first account creation.
- [ ] ID.me callback validates state and PKCE server-side.
- [ ] ID.me token exchange and UserInfo/claims handling remain server-side.
- [ ] Verified identity/contact fields are stored safely for confirmation and later matching.
- [ ] Existing account/profile linking is safe and does not leak whether an email already exists.
- [ ] One ID.me subject cannot create multiple active PassTo profiles without approved recovery/linking rules.
- [ ] Lovable receives only safe route/status/contact-confirmation data.
- [ ] Audit events are written for success and failure paths.
- [ ] Claude documents migrations required, files changed, tests run, deviations, and risks.

## QA Plan

- Test a fresh ID.me sandbox verification with no existing profile.
- Test ID.me callback with invalid state.
- Test ID.me callback with invalid or missing code verifier.
- Test repeated callback/idempotency behavior.
- Test existing email/profile linking behavior without leaking account existence.
- Test duplicate ID.me subject handling.
- Confirm Lovable cannot perform token exchange or write identity status directly.
- Review Supabase logs and audit rows for all success/failure paths.

## David Approval Request

David approval is required before Claude executes this Class A/security-sensitive implementation task.

---

## Codex Security Review — 2026-05-31

**Reviewer:** Codex  
**Scope:** C-1 through C-3 security questions for ID.me-first onboarding  
**Result:** Approved with required constraints  
**Execution Approval:** Still pending David approval  

### C-1 — Approved validation pattern for unauthenticated ID.me callback

`idme-exchange-v2` may use `verify_jwt = false` only because the callback is pre-account. That setting removes Supabase's platform JWT gate, so the function must implement its own server-side validation before doing any token exchange, profile write, or Auth write.

Approved pattern:

```text
/id-verification/start
  creates onboarding_attempt
  generates state and PKCE verifier server-side
  stores only state_hash and encrypted/sealed PKCE verifier
  redirects to ID.me with state + S256 code_challenge

/auth/idme/callback
  verify_jwt = false
  accepts only ID.me code + state
  hashes received state and looks up one pending, unexpired attempt
  atomically marks attempt callback_received / locks attempt
  decrypts PKCE verifier server-side
  exchanges code with ID.me server-side
  validates ID.me response and assurance level
  stores verified identity result on the attempt
  redirects to /confirm-info with opaque attempt reference only
```

Required details:

- Use `state` as the primary CSRF/session correlation value.
- Store `state_hash`, not raw `state`.
- Use high-entropy random state; do not encode email, phone, name, or plan in state.
- Use PKCE with `code_challenge_method = S256`.
- The token exchange requires the raw `code_verifier`; therefore a hash-only verifier is not sufficient. Store the verifier encrypted/sealed with a server-only secret, or use an equivalent server-only sealed value. Do not expose the verifier to Lovable.
- Validate attempt status before token exchange: only `started` / `pending_callback` and not expired.
- Mark the attempt consumed/used atomically so callback replay cannot exchange or link twice.
- Reject missing, expired, reused, or mismatched state with a generic error page and audit event.
- Do not trust any caller-supplied profile/account identifiers on callback.
- Redirect only to approved first-party routes. Do not accept arbitrary `next` / redirect URLs from the browser.
- Do not include authorization code, state, access token, ID token, or provider details in user-facing redirects after callback handling.
- Do not write `profiles.id_verification_status`, create Auth users, or link profiles until the attempt has a validated ID.me result.

Implementation note: this may be one Edge Function with route branching or two Edge Functions. If split, only the provider callback must be `verify_jwt = false`; post-callback confirm/link actions should use an attempt-bound token plus server checks until a Supabase session exists.

### C-2 — `onboarding_attempts` schema requirements

The proposed `onboarding_attempts` model is the right direction, but a partial unique index for active attempts is not sufficient by itself.

Required fields:

```text
id uuid primary key
status text
state_hash text unique not null
code_verifier_ciphertext text not null
code_challenge_hash text
redirect_uri text not null
idme_subject text
id_verification_level text
email text
phone text
first_name text
last_name text
linked_auth_user_id uuid
linked_profile_id uuid
expires_at timestamptz not null
consumed_at timestamptz
created_at timestamptz not null
updated_at timestamptz not null
last_error_key text
attempt_ip inet
attempt_user_agent text
```

Required constraints/indexes:

- `state_hash` unique.
- Status check such as:

```text
started
callback_received
verified
linked
expired
failed
abandoned
```

- `expires_at > created_at`.
- `consumed_at` required when status is `verified` or `linked`.
- Partial unique index to prevent multiple active attempts for the same resolved ID.me subject after subject is known:

```text
unique (idme_subject)
where idme_subject is not null
  and status in ('verified', 'linked')
```

- Unique constraint or partial unique index preventing one ID.me subject from linking to multiple active profiles, either on `profiles.id_me_subject` or through an approved identity-link table.
- Partial index for cleanup/lookup:

```text
(expires_at) where status in ('started', 'callback_received', 'verified')
```

- No RLS policy that lets anonymous users read `onboarding_attempts`.
- No client-side direct insert/update to `onboarding_attempts`.
- Service-role only writes through Edge Functions.

Open design choice:

- Codex prefers an unexposed/server-only table. If it must be in `public`, RLS must be enabled with no anon/auth direct access except narrow server-controlled RPCs, but a private schema is cleaner if compatible with the existing Supabase project conventions.

Data minimization:

- Store only fields needed for confirmation and matching.
- Do not store raw provider access tokens unless absolutely required. If stored temporarily for debugging/provider follow-up, they must be encrypted, short-lived, and cleared after use.
- Store DOB or other sensitive matching attributes only if required for license matching, encrypted or otherwise minimized, and clear them after matching where possible.

### C-3 — Account creation mechanism

`supabase.auth.admin.createUser()` using the service-role client is approved for this ID.me-first flow, with constraints.

Rationale:

- The user is not authenticated yet, so browser-side `signUp()` does not fit the post-ID.me account creation moment cleanly.
- The account must be created or linked only after the backend has validated the ID.me attempt.
- The service-role key must remain server-side in the Edge Function.

Required constraints:

- Use `admin.createUser()` only after a validated ID.me attempt and contact confirmation.
- Do not use browser-supplied email/phone as trust source without comparing to the verified attempt and the nurse's confirmation step.
- Do not set `phone_confirm: true` from ID.me-returned phone. Phone possession still requires Twilio.
- `email_confirm: true` is allowed only if David/Codex explicitly accept ID.me email as sufficient for account email verification. Otherwise create the user and send a Supabase invite/magic-link/confirmation path.
- Do not set a user-chosen password in this pre-account flow unless a later approved UX requires it. Prefer passwordless/magic-link or generated recovery/invite flow.
- Store authorization/role/linking state in profile/application tables or app metadata where appropriate, not in user-editable metadata.
- Avoid account enumeration: existing-account conflicts must produce a generic recovery/linking message and support path.
- Profile creation/linking must be idempotent and protected by database uniqueness on `auth_user_id` and `id_me_subject`.

### Approved Direction

Codex approves this direction for TASK-0045 planning:

```text
verify_jwt = false callback
        +
server-created onboarding_attempt
        +
state_hash lookup
        +
server-held encrypted PKCE verifier
        +
single-use attempt consumption
        +
service-role admin.createUser/link after verified attempt
```

Claude must document the exact SQL and Edge Function changes before execution. David approval remains required before applying migrations, deploying functions, or changing the live Lovable flow.

## Claude Implementation Notes

_To be completed by Claude after execution._

## Codex QA Review

### Codex Security QA — 2026-05-31

**Reviewer:** Codex  
**Scope:** `idme-exchange-v2`, `create-account`, Migration E/F live schema  
**Live Supabase Project Checked:** `wvzjfxacykgsaffskgtr`  
**Deployed Functions Checked:** `idme-exchange-v2` v2, `create-account` v2  
**Verdict:** Blocked

The two new functions are deployed with `verify_jwt = false`, as required for a pre-account flow, but the replacement validation is not sufficient yet. The current implementation does not meet the previously approved server-side state/PKCE pattern recorded in this task.

Live schema notes:

- `public.onboarding_attempts` exists with RLS enabled and no anon/auth policies. Service-role writes should bypass RLS.
- `public.onboarding_attempts` does not contain `state_hash`, `pkce_state_hash`, `code_verifier_ciphertext`, `consumed_at`, or server-side redirect/state binding fields.
- `public.onboarding_attempts` has a partial unique index on `idme_subject` where `state` is not `linked`, `abandoned`, or `expired`.
- `public.profiles.onboarding_step` CHECK includes `identity`, `confirm`, `license`, `phone`, `plan`, `selfie`, `pass`, and `complete`.
- `public.audit_events.actor_id` is nullable and references `profiles(id) ON DELETE SET NULL`; null actor audit rows are schema-compatible.

#### P1 — `idme-exchange-v2`: unauthenticated callback lacks server-side state validation

Pattern: `supabase/functions/idme-exchange-v2/index.ts` lines 42-73, 199-259

What is wrong:

- The function accepts only `code` and `code_verifier` from the browser.
- It does not accept, hash, or validate the OAuth `state` parameter server-side.
- It does not look up a pre-created pending `onboarding_attempt` before token exchange.
- It creates the `onboarding_attempts` row only after the provider token exchange and attributes call have already succeeded.
- PKCE format validation is not enough. Because the `code_verifier` is held in Lovable `sessionStorage`, the server cannot independently prove the callback belongs to a server-created enrollment attempt.

Attack surface opened by `verify_jwt = false`:

- Any internet caller can invoke the function directly; CORS only limits browser read access, not server-to-server invocation.
- The endpoint becomes a public ID.me code exchange endpoint for the PassTo client.
- CSRF/login-injection and callback-mixup protections depend on client-side state only.
- There is no server-side replay/consumption record for the authorization start event.

Required fix:

- Add a trusted `/id-verification/start` or equivalent backend branch that creates `onboarding_attempts` before redirecting to ID.me.
- Generate high-entropy `state` and PKCE verifier server-side.
- Store `state_hash` and an encrypted/sealed `code_verifier` or server-only equivalent.
- On callback, require `code + state`, hash `state`, find exactly one pending unexpired attempt, atomically mark it callback-received/consumed, then exchange the code with the stored verifier.
- Do not perform ID.me token exchange until server-side attempt/state validation passes.

#### P1 — `create-account`: account creation trusts browser-supplied email and pre-confirms it

Pattern: `supabase/functions/create-account/index.ts` lines 52-83, 86-90, 138-145

What is wrong:

- The function accepts `confirmed_email` from the browser and uses it as the Auth account email.
- The attempt lookup does not select `verified_email`, so the function never compares browser-submitted email to the ID.me-returned email.
- The function sets `email_confirm: true` for this browser-submitted email.
- A caller who completed ID.me for themselves could create a PassTo account under any unused email address and receive an immediate session token.

Required fix:

- Select `verified_email` from `onboarding_attempts`.
- If the nurse keeps the ID.me email, require exact normalized match before using `email_confirm: true`.
- If the nurse edits email, create the user without pre-confirming email and require Supabase email verification/magic-link possession before returning a session or advancing.
- Do not return a session for an email address that has not been verified or explicitly accepted as verified by the backend.

#### P1 — `create-account`: attempt consumption and profile linking are not atomic

Pattern: `supabase/functions/create-account/index.ts` lines 85-132, 138-228

What is wrong:

- The function reads an `id_verified` attempt, creates an Auth user, updates the profile, then marks the attempt `linked`.
- The attempt is not atomically claimed before account creation.
- Concurrent calls with the same `attempt_id` can both pass the initial state/profile checks before either marks the attempt linked.
- `profiles.id_me_subject UNIQUE` will stop final duplicate verified profiles, but only after Auth users and trigger-created profiles may already exist.
- The cleanup path uses best-effort `admin.deleteUser()` only. If the function times out, the network fails, or `deleteUser()` fails, partial Auth/profile state can persist.
- The attempt update to `linked` ignores `{ error }`, so the function can return a session even if the attempt remains active/stale.

Required fix:

- Atomically claim the attempt before `admin.createUser()`, for example `UPDATE onboarding_attempts SET state = 'account_creating' WHERE id = $1 AND state = 'id_verified' AND expires_at > now() RETURNING ...`.
- Add `account_creating` or equivalent to the state machine, with a recovery/expiry path.
- After Auth user creation, use a database RPC/transaction to update `profiles`, mark the attempt `linked`, and insert the success audit event together.
- Check every Supabase `{ error }` result.
- Add an operational remediation path for orphaned Auth/profile rows instead of relying only on best-effort immediate deletion.

#### P2 — `onboarding_attempts`: partial unique index has expiry and lifecycle gaps

Pattern: live index `onboarding_attempts_active_subject_idx`; `idme-exchange-v2` lines 206-259

What is wrong:

- The index excludes only rows whose `state` is `linked`, `abandoned`, or `expired`; it does not exclude rows whose `expires_at` is in the past.
- `idme-exchange-v2` checks whether an existing attempt is expired and then attempts to insert a new row, but the old expired-by-time row still participates in the unique index until its `state` is updated to `expired`.
- This can cause stale attempts to block new enrollment attempts for the same ID.me subject.
- The schema lacks `consumed_at` and database constraints tying terminal states to profile linkage/consumption.

Required fix:

- Before inserting a new attempt, mark expired active attempts for that subject as `expired`, or perform this through a single RPC.
- Add a cleanup job or function for expired attempts.
- Add lifecycle constraints such as `expires_at > created_at`, `consumed_at` for linked/consumed states, and a state transition model that distinguishes `id_verified`, `account_creating`, `linked`, `expired`, `failed`, and `abandoned`.
- Keep `profiles.id_me_subject UNIQUE`; it is still the final duplicate-profile backstop.

#### P2 — `create-account`: duplicate email detection relies on fragile message/status matching

Pattern: `supabase/functions/create-account/index.ts` lines 147-152

What is wrong:

- The code checks whether the error message contains `already` or `exists`, and treats any `status === 422` as duplicate.
- Supabase Auth exposes structured Auth error codes; the current duplicate email code is `email_exists`.
- Treating all `422` responses as duplicates can misclassify other validation/configuration failures and hide real setup issues.

Required fix:

- Detect duplicate email using the structured Auth error code:

```ts
const isDuplicate = createUserErr.code === "email_exists";
```

- If using a compatibility fallback, keep it narrow and log the full error server-side.
- Do not classify every `422` as a duplicate.

#### P2 — `create-account`: returned `hashed_token` is a bearer login credential

Pattern: `supabase/functions/create-account/index.ts` lines 240-263

What is wrong:

- `generateLink({ type: "magiclink" })` returns a token hash that can be exchanged with `verifyOtp()` for a Supabase session.
- Returning that token in a JSON body is effectively returning a one-time bearer login credential to browser JavaScript.
- HTTPS protects transport, but it does not protect against XSS, browser extension access, frontend logging, response caching, or misuse after `attempt_id` leakage.
- This is especially risky while `create-account` is gated only by an unbound `attempt_id`.

Required fix:

- Preferred: send the magic link to the verified email, or use a server-side `/auth/confirm` route that exchanges the token and sets secure, `HttpOnly`, `SameSite` cookies.
- If a SPA response-body token remains temporarily necessary, first fix server-side state/attempt binding and atomic consumption, then add `Cache-Control: no-store`, avoid logging the token, and immediately exchange it with `verifyOtp({ token_hash, type: "email" })`.
- Do not call the returned value a `session_token`; it is an OTP token hash, not a Supabase session.

#### P2 — `idme-exchange-v2`: provider response validation is not pinned enough for production

Pattern: `supabase/functions/idme-exchange-v2/index.ts` lines 121-140, 147-178

What is wrong:

- IAL/subject extraction is still heuristic across several possible fields.
- Diagnostic logging of provider response shape remains enabled.
- The function does not validate an ID.me issuer/audience-bound ID token or a pinned expected UserInfo/Attributes response contract.

Required fix:

- Pin the exact ID.me sandbox/production response fields before production.
- Validate the expected issuer/audience/subject source for the ID.me result, or document why the chosen ID.me endpoint is authoritative.
- Remove diagnostic provider-shape logging before production.
- Fail closed if the expected assurance/subject fields are absent or ambiguous.

#### P3 — both functions: CORS is useful but incomplete and must not be treated as authentication

Pattern: `supabase/functions/idme-exchange-v2/index.ts` lines 22-34; `supabase/functions/create-account/index.ts` lines 27-39

What is wrong:

- `Access-Control-Allow-Origin` is restricted to `https://enroll.passtodigital.com`, which is good for browser clients.
- The `OPTIONS` response does not include `Access-Control-Allow-Methods`.
- CORS does not prevent non-browser clients from invoking `verify_jwt = false` functions.

Required fix:

- Add at least:

```ts
"Access-Control-Allow-Methods": "POST, OPTIONS"
```

- Consider `Vary: Origin` and `Cache-Control: no-store`.
- Keep all real authorization in server-side state/attempt validation, not CORS.

#### P3 — both functions: Supabase insert/update errors are not consistently inspected

Pattern: `idme-exchange-v2` lines 263-276; `create-account` lines 220-234 and 281-288

What is wrong:

- Supabase client calls return `{ error }`; they do not necessarily throw.
- Audit writes and the `onboarding_attempts` linked update can fail silently.

Required fix:

- Inspect `{ error }` on every Supabase call.
- For success-path audit/linking, fail closed or return a recovery state if the write is part of the trust boundary.
- Keep non-blocking audit only for explicitly approved failure telemetry.

#### Validated Items

- `audit_events.actor_id = null` is schema-compatible. The column is nullable and the FK is `ON DELETE SET NULL`.
- Service-role inserts into `audit_events` and `onboarding_attempts` should bypass RLS. RLS is enabled and no anon/auth policies are present.
- Broad table grants to `anon` and `authenticated` exist on `onboarding_attempts` and `audit_events`, but RLS currently blocks access because there are no policies. Codex recommends revoking unnecessary anon/auth grants as defense in depth.

### Required Before Approval

- Implement server-side state + PKCE storage/validation before ID.me token exchange.
- Bind `create-account` to a server-validated, atomically claimed attempt.
- Stop pre-confirming browser-edited email.
- Replace fragile duplicate-email detection with `email_exists`.
- Resolve the session-establishment pattern and avoid returning bearer login material unless the attempt model is fixed and response hardening is added.
- Make profile update, attempt linking, and success audit atomic or explicitly recoverable.

**Final Verdict:** Blocked — production use is not approved until the P1/P2 findings are remediated and re-reviewed.


---

## Claude Engineering Review — 2026-05-31

**Reviewer:** Claude / Senior Engineer  
**Review Status:** Complete — Awaiting David Approval and Codex Security Review before execution  
**Verdict:** Feasible — requires schema migration and `idme-exchange` rewrite before implementation

### 1. Technical Feasibility

The ID.me-first flow is technically feasible. The core challenge is bridging the gap between ID.me verification completing and a Supabase Auth account existing. The `profiles` table requires `auth_user_id NOT NULL`, so no profile row can exist before a Supabase Auth user is created. A pre-account state mechanism is required.

### 2. Schema Changes Required

**`onboarding_attempts` table is needed.** A JWT-only stateless approach is insufficient because:
- Server-side `UNIQUE(idme_subject)` prevents duplicate active enrollments from the same ID.me identity
- Resumability requires persisted state
- Abandoned/expired attempts need to be auditable

**Proposed Migration E SQL:**

```sql
CREATE TABLE public.onboarding_attempts (
  id                    uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  id_verification_level text,
  verified_first_name   text,
  verified_last_name    text,
  verified_email        text,
  verified_phone        text,
  pkce_state_hash       text NOT NULL,
  state                 text NOT NULL DEFAULT 'started'
                        CHECK (state IN ('started','id_verified','linked','abandoned','expired')),
  profile_id            uuid REFERENCES public.profiles(id) ON DELETE SET NULL,
  expires_at            timestamptz NOT NULL DEFAULT (now() + interval '30 minutes'),
  created_at            timestamptz NOT NULL DEFAULT now(),
  updated_at            timestamptz NOT NULL DEFAULT now()
);

-- Partial unique index: one active attempt per ID.me subject
CREATE UNIQUE INDEX onboarding_attempts_active_idme_subject_idx
  ON public.onboarding_attempts (id_me_subject)
  WHERE state NOT IN ('linked', 'abandoned', 'expired');

ALTER TABLE public.onboarding_attempts ENABLE ROW LEVEL SECURITY;
-- No authenticated client policies. Service-role only.
```

**Migration F also required:** Update `onboarding_step` CHECK constraint. New flow ordering is `identity → confirm → license → phone → plan → selfie → pass → complete`. Current constraint assumes `identity → phone → license`. This breaks `phone-send-otp` / `phone-verify-otp` which check `onboarding_step = 'phone'` before `license`.

### 3. Auth/Profile Linking Strategy

Recommended: service-role `admin.createUser()` called from a new `create-account` Edge Function after `/confirm-info`. The `handle_new_user()` trigger fires and creates the `profiles` row. The Edge Function immediately updates the row with verified fields. Magic link (not password) recommended for initial session — eliminates the password-first UX problem.

### 4. ID.me Subject Uniqueness

Two-layer approach:
- `onboarding_attempts` partial unique index (active attempts only) prevents simultaneous duplicate enrollments
- `profiles.id_me_subject UNIQUE` (already in v4) prevents final duplicate profile creation
- Conflict errors must not leak whether an email belongs to another user

### 5. Security/RLS Risks

Five risks identified:

| Risk | Severity | Mitigation |
|---|---|---|
| `idme-exchange` currently uses `verify_jwt: true` — incompatible with pre-account flow | High | New `idme-exchange-v2` with `verify_jwt: false` and custom PKCE+state validation. Requires Codex security review. |
| PKCE/state must be stored server-side, not just sessionStorage | High | `onboarding_attempts.pkce_state_hash` — hash of state stored at attempt creation |
| Nurse can change email at `/confirm-info`; collision with existing Supabase account | High | `create-account` Edge Function checks email existence without revealing it; writes audit event |
| `verified_phone` from ID.me must not be written to `profiles.phone` without Twilio OTP | High | `create-account` must not set `profiles.phone`; only `phone-verify-otp` may do so |
| `onboarding_attempts` must be service-role only — no authenticated client access | Medium | RLS enabled, no client policies defined |

### 6. Impact on TASK-0025

`idme-exchange` needs a rewrite. It currently:
- Requires `verify_jwt: true` (incompatible with pre-account flow)
- Writes directly to `profiles`
- Has no `onboarding_attempts` concept

Recommended: build new `idme-exchange-v2`. Bundle `IDME_ATTRIBUTES_URL` fix into v2. Deprecate existing function once v2 is validated.

### 7. Recommended Execution Order

Pre-execution gates (David approval required):
1. Approve Migration E (`onboarding_attempts`)
2. Approve Migration F (new `onboarding_step` CHECK constraint values)
3. Codex security review of `verify_jwt: false` endpoint model and account-linking rules
4. Decision on TASK-0022 / Phase 2 Lovable work obsolescence

If gates clear:
1. Apply Migration E → Migration F → David
2. Deploy `idme-exchange-v2` (pre-account) → Claude
3. Deploy `create-account` Edge Function → Claude
4. TASK-0045 complete → Codex QA
5. TASK-0046 (license binding) → Claude → Codex QA
6. Update Twilio functions for new step ordering → Claude
7. TASK-0047 (routing reorder) → Claude → Codex QA
8. Lovable prompt execution → David

### 8. Approval Requests

**David decisions needed:**
- D-1: Approve Migration E SQL (onboarding_attempts table)?
- D-2: Approve new `onboarding_step` CHECK values (identity → confirm → license → phone → plan → selfie → pass → complete)?
- D-3: Abandon TASK-0022 / Phase 2 Lovable work in favor of the new Lovable prompt?
- D-4: Magic link vs. password for account creation after ID.me?
- D-5: Bundle `IDME_ATTRIBUTES_URL` fix into `idme-exchange-v2`?

**Codex security review needed:**
- C-1: Approved pattern for `verify_jwt: false` endpoint with custom state/PKCE validation?
- C-2: `onboarding_attempts` schema and partial unique index sufficient?
- C-3: `admin.createUser()` approved as the account creation mechanism?

---

## P1 Remediation — 2026-06-01 — Claude

**Status:** Complete — pushed to GitHub; awaiting Migration E + deployment

### What Changed

All three P1 findings from Codex QA have been addressed.

#### 1. `idme-verification-start` (new function — deployed after Migration E)

- Generates `state` and `code_verifier` server-side (32 bytes and 48 bytes respectively)
- Stores `state_hash` (SHA-256 hex of state) and `code_verifier_ciphertext` (AES-GCM, key from `ONBOARDING_ENCRYPTION_KEY` secret) in `onboarding_attempts`
- Returns `attempt_id`, `state`, `code_challenge`, `code_challenge_method` to Lovable
- Lovable stores only `attempt_id` in sessionStorage; state comes back via ID.me URL param
- Adds key length validation (16/24/32 bytes); supports both `base64:` and `base64:=` prefixes
- Writes `identity.verification_started` audit event (non-blocking)

#### 2. `idme-exchange-v2` (full rewrite)

Input changed from `{ code, code_verifier }` → `{ attempt_id, code, state }`.

| P1 finding | Fix |
|---|---|
| Accepted `code_verifier` from browser | Looks up `code_verifier_ciphertext` from DB; decrypts server-side |
| No state validation | Hashes received `state` and compares to `attempt.state_hash` with `constantTimeEqual()` |
| No attempt lookup before exchange | Requires `attempt_id`; validates state in `started`, expiry, and both PKCE fields present |
| No atomic consume before exchange | `UPDATE SET consumed_at=now() WHERE consumed_at IS NULL` — rejects if 0 rows returned |
| Created attempt after exchange | Attempt must pre-exist (created by `idme-verification-start`) |
| No abandon on provider failure | Every exchange/attributes failure path calls `markAbandoned()` |

#### 3. `create-account` (P1 fixes)

| P1 finding | Fix |
|---|---|
| No atomic claim before `createUser()` | `UPDATE SET state='account_creating' WHERE state='id_verified'` — returns 409 if 0 rows |
| `verified_email` not selected | `SELECT` now includes `verified_email` from attempt |
| `email_confirm: true` always | `email_confirm = (confirmed_email == attempt.verified_email)` — case-insensitive comparison |
| Profile update + link not recoverable | Every failure path resets attempt to `id_verified` (best-effort); attempt link failure is non-fatal and logged |
| Fragile duplicate email detection | Changed to `createUserErr.code === "email_exists"` |
| No session if email unconfirmed | Returns `{ email_confirmed: false, next: "verify_email" }` instead of a session token |

#### 4. Migration E (revised)

File: `supabase/migrations/migration_e_onboarding_attempts_v2.sql`

- `ALTER TABLE` (not `CREATE TABLE`) — table already exists
- `ADD COLUMN IF NOT EXISTS state_hash text`
- `ADD COLUMN IF NOT EXISTS code_verifier_ciphertext text`
- `ADD COLUMN IF NOT EXISTS consumed_at timestamptz`
- Updated `state` CHECK to include `'account_creating'`
- Dropped old partial unique index; recreated with `expires_at > now()` guard
- Added `cleanup_expired_onboarding_attempts()` function

Migration F (`onboarding_step` constraint) is already applied per Codex QA live schema check.

### Pre-Deployment Requirements

**David must complete before functions are deployed:**

1. Apply `supabase/migrations/migration_e_onboarding_attempts_v2.sql` via Supabase dashboard SQL Editor
2. Set `ONBOARDING_ENCRYPTION_KEY` secret on the `idme-verification-start` and `idme-exchange-v2` functions:
   - Generate a 32-byte random key: `openssl rand -base64 32`
   - Prefix with `base64:` when setting in Supabase dashboard
   - Same key must be set on both functions (start encrypts, exchange decrypts)

**Deploy commands (after migration and secrets):**
```
npx supabase login   # one-time if not already logged in
npx supabase functions deploy idme-verification-start --project-ref wvzjfxacykgsaffskgtr --no-verify-jwt
npx supabase functions deploy idme-exchange-v2 --project-ref wvzjfxacykgsaffskgtr --no-verify-jwt
npx supabase functions deploy create-account --project-ref wvzjfxacykgsaffskgtr --no-verify-jwt
```

### GitHub Files Changed

| File | Change |
|---|---|
| `supabase/functions/idme-verification-start/index.ts` | New function — P1 server-side PKCE start |
| `supabase/functions/idme-exchange-v2/index.ts` | Full rewrite — server-side state/PKCE validation |
| `supabase/functions/create-account/index.ts` | P1 fixes — atomic claim, email comparison, recovery |
| `supabase/migrations/migration_e_onboarding_attempts_v2.sql` | Revised Migration E — ALTER TABLE |

### Open David Decisions (D-3, D-4 still open)

| # | Decision | Status |
|---|---|---|
| D-1 | Approve Migration E SQL | ✅ Revised — ready to apply (see migration file) |
| D-2 | Approve `onboarding_step` values | ✅ Already applied per Codex QA schema check |
| D-3 | Abandon TASK-0022 / Phase 2 Lovable work? | ⬜ Pending David |
| D-4 | Magic link vs. password for account creation? | ✅ Magic link implemented |
| D-5 | Bundle `IDME_ATTRIBUTES_URL` fix into `idme-exchange-v2`? | ✅ IDME_ATTRIBUTES_URL is read from env secret — David sets the correct URL |

### Codex Re-QA Request

Codex should re-review `idme-verification-start`, `idme-exchange-v2`, and `create-account` against the P1 findings above.

Key review areas for re-QA:
- `constantTimeEqual()` — adequate for state_hash comparison?
- `consumed_at IS NULL` atomic lock — race condition coverage sufficient?
- Decrypt error handling — no ciphertext leakage?
- `email_confirm` logic — correct boundary conditions?
- `account_creating` → `id_verified` reset path — safe under all failure modes?
- `ONBOARDING_ENCRYPTION_KEY` handling — key not logged anywhere?

Apply labels on this task: `assigned: codex`, `needs: codex-review`, `status: ready-for-codex-qa`

---

## Codex Re-QA — 2026-06-01

**Reviewer:** Codex  
**Scope:** GitHub source after Claude P1 remediation, revised Migration E, live Supabase schema, live Edge Function deployment  
**Live Supabase Project Checked:** `wvzjfxacykgsaffskgtr`  
**Live Functions Checked:** `idme-exchange-v2` v3, `create-account` v3  
**Verdict:** Blocked

### Summary

The GitHub source is directionally much closer to the approved security model: `idme-verification-start` now creates the pre-account attempt, `idme-exchange-v2` expects `{ attempt_id, code, state }`, validates `state_hash`, decrypts a server-held PKCE verifier, and uses `consumed_at IS NULL` as a replay guard. `create-account` now has the right broad shape for atomic attempt claiming and conditional email confirmation.

TASK-0045 is still blocked because the live project and migration do not currently support that source. The live deployed `idme-exchange-v2` and `create-account` are still the earlier insecure implementations, and the revised Migration E would not make the live `onboarding_attempts` table compatible with the new `idme-verification-start` flow.

### Live State Observed

- `idme-verification-start` is not deployed.
- Live `idme-exchange-v2` v3 is `verify_jwt = false` but still accepts browser-supplied `{ code, code_verifier }` and does not validate server-side state before token exchange.
- Live `create-account` v3 is `verify_jwt = false` but still trusts browser-supplied email, sets `email_confirm: true`, does not atomically claim `id_verified -> account_creating`, and uses fragile duplicate-email detection.
- Live `public.onboarding_attempts` does not have `state_hash`, `code_verifier_ciphertext`, or `consumed_at`.
- Live `public.onboarding_attempts.state` CHECK does not include `account_creating`.
- Live `public.onboarding_attempts.idme_subject` and `id_verification_level` are `NOT NULL`.
- Live `public.onboarding_attempts` still has the old partial unique index on active `idme_subject`.

### Findings

#### P1 — Live deployment is still the pre-remediation unauthenticated flow

Function/pattern: live `idme-exchange-v2` v3 and live `create-account` v3

What is wrong:

- The deployed `idme-exchange-v2` still accepts `code_verifier` from the browser and does not require a server-created attempt with `state_hash`.
- The deployed `create-account` still pre-confirms browser-submitted email and does not atomically claim the attempt before Auth user creation.
- Both deployed functions are public `verify_jwt = false` functions, so the old attack surface remains live until replaced or disabled.

Required fix:

- Do not use the live v3 functions for production or Lovable QA.
- Apply a corrected Migration E first.
- Deploy `idme-verification-start`.
- Redeploy `idme-exchange-v2` and `create-account` from the remediated GitHub source.
- After deployment, verify the live function source/behavior again, not only the GitHub files.

#### P1 — Revised Migration E is incompatible with the current live table and new start function

Pattern: `supabase/migrations/migration_e_onboarding_attempts_v2.sql` lines 14-33; `supabase/functions/idme-verification-start/index.ts` lines 62-69

What is wrong:

- The live table has `idme_subject NOT NULL` and `id_verification_level NOT NULL`.
- The new `idme-verification-start` function inserts a `started` row before ID.me has returned either `idme_subject` or `id_verification_level`.
- The revised migration adds `state_hash`, `code_verifier_ciphertext`, and `consumed_at`, but does not relax those two existing `NOT NULL` constraints or replace them with state-aware constraints.
- Therefore, even if the revised migration partially applied, `idme-verification-start` would fail on insert.

Required fix:

- Revise Migration E so pre-callback `started` attempts can have `idme_subject` and `id_verification_level` as null.
- Add state-aware constraints instead, for example:
  - `state_hash` and `code_verifier_ciphertext` required for `started` attempts.
  - `idme_subject` and `id_verification_level` required once state is `id_verified`, `account_creating`, or `linked`.
  - `profile_id` required when state is `linked`.
- Re-run Codex review on the revised SQL before applying it.

#### P1 — Revised Migration E uses `now()` in a partial index predicate

Pattern: `supabase/migrations/migration_e_onboarding_attempts_v2.sql` lines 40-45

What is wrong:

- The migration attempts to create a partial unique index with `expires_at > now()`.
- Postgres index predicates cannot depend on a time-varying function like `now()`. This migration is expected to fail when applied.
- Even conceptually, a row aging past `expires_at` would not automatically leave a partial index without an update.

Required fix:

- Remove `expires_at > now()` from the partial index.
- Use explicit state transitions for expiry: mark stale attempts `expired` before creating/claiming new attempts for the same subject, and/or run `cleanup_expired_onboarding_attempts()`.
- Keep the unique index based on durable state only, for example a partial index on `idme_subject` where state is in active non-terminal states and `idme_subject is not null`.

#### P2 — `create-account` still lets browser-edited names become profile identity names

Pattern: `supabase/functions/create-account/index.ts` lines 80-88 and 235-244

What is wrong:

- The remediated source selects `verified_email`, but not `verified_first_name` or `verified_last_name`.
- It writes browser-submitted `first_name` and `last_name` into `profiles` while also setting `id_verification_status = 'verified'`.
- If later license binding reads `profiles.first_name` / `profiles.last_name` as the verified ID.me identity, the nurse could self-attest the name used for license matching.

Required fix:

- Select `verified_first_name` and `verified_last_name` from the attempt.
- Preserve ID.me-returned names as the trust source for later license matching.
- If the nurse is allowed to edit display/contact names, store those separately or clearly mark them as unverified; do not let browser-edited names become the identity matching source.

#### P2 — Account creation/linking is improved but still not atomic enough for approval

Pattern: `supabase/functions/create-account/index.ts` lines 235-279

What is wrong:

- Profile update, attempt link, and success audit are still separate writes.
- The attempt link failure is treated as non-fatal, so the function can return success after creating a verified profile while the attempt remains in `account_creating`.
- `writeAudit(...)` is not awaited, so success audit failure is not visible to the response path.

Required fix:

- Prefer an RPC/transaction that updates the profile, marks the attempt `linked`, and writes the success audit event together.
- If a full transaction is deferred, fail closed on attempt-link failure or return an explicit recovery state.
- Await the success audit write if it is part of the trust-boundary record.

#### P2 — Returning the magic-link token hash in JSON remains a production concern

Pattern: `supabase/functions/create-account/index.ts` lines 299-327

What is wrong:

- The function still returns `linkData.properties.hashed_token` to browser JavaScript as `session_token`.
- Supabase documents `generateLink()` as an admin method for trusted server use and `verifyOtp()` as the method that can exchange a `token_hash` into a login session. That makes this value one-time bearer login material.
- The remediated `create-account` `json()` helper does not set `Cache-Control: no-store`.

Required fix:

- Preferred: send the magic link or use a backend callback that sets secure, `HttpOnly`, `SameSite` cookies.
- If the SPA token-hash handoff is temporarily accepted, add `Cache-Control: no-store`, never log it, call it `token_hash` rather than `session_token`, and require immediate client exchange via `verifyOtp`.
- Revisit this before production launch.

#### P3 — Grants on `onboarding_attempts` remain broader than needed

Pattern: live `public.onboarding_attempts` table grants

What is wrong:

- Live grants still include broad privileges for `anon` and `authenticated`.
- RLS currently blocks access because no policies exist, but this table should be defense-in-depth service-role only.

Required fix:

- Revoke unnecessary `anon` and `authenticated` grants on `public.onboarding_attempts`.
- Keep all writes mediated through Edge Functions/service-role paths.

### Validated / Improved Items

- The remediated GitHub `constantTimeEqual()` implementation is adequate for fixed-length SHA-256 hex comparison.
- The remediated GitHub PKCE model is directionally correct: the browser no longer supplies `code_verifier` to `idme-exchange-v2`.
- The remediated GitHub `consumed_at IS NULL` compare-and-swap pattern is a reasonable replay guard once the column exists and the function is deployed.
- AES-GCM key handling does not log raw ciphertext or plaintext verifier values.
- Conditional `email_confirm` based on normalized match to `verified_email` is the right direction.

### Required Before Approval

- Revise Migration E for the live table shape, including nullability/state-aware constraints and a valid unique-index strategy.
- Apply the corrected migration only after David approval.
- Deploy `idme-verification-start`, then redeploy `idme-exchange-v2` and `create-account` from the remediated GitHub source.
- Ensure live function code matches reviewed source.
- Preserve verified ID.me names as the trust source for TASK-0046 license matching.
- Re-run Codex QA against the live schema and deployed functions.

**Final Verdict:** Blocked — the remediation direction is good, but TASK-0045 is not approved for production or downstream TASK-0046/TASK-0047 execution until the migration and live deployment issues above are fixed and re-reviewed.

---

## Second Remediation — 2026-06-01 — Claude

**Status:** Complete — pushed to GitHub; awaiting Migration E (v3) + deployment + Codex re-QA

### Fixes Applied

#### Migration E (v3) — file: `supabase/migrations/migration_e_onboarding_attempts_v2.sql`

| P1/P2 finding | Fix |
|---|---|
| `idme_subject NOT NULL` incompatible with `started` row | `ALTER COLUMN idme_subject DROP NOT NULL` |
| `id_verification_level NOT NULL` incompatible with `started` row | `ALTER COLUMN id_verification_level DROP NOT NULL` |
| `expires_at > now()` invalid in Postgres partial index predicate | Removed — index predicate is now durable state only |
| Excess `anon` / `authenticated` grants on `onboarding_attempts` | `REVOKE ALL … FROM anon; REVOKE ALL … FROM authenticated;` |
| No state-aware constraints on field requirements | Added three CHECK constraints: `started` requires PKCE fields; `id_verified`/`account_creating`/`linked` require `idme_subject` + `id_verification_level`; `linked` requires `profile_id` |

#### `create-account` — file: `supabase/functions/create-account/index.ts`

| P2 finding | Fix |
|---|---|
| Browser-submitted names written as identity anchor | Profile update now uses `attempt.verified_first_name` / `attempt.verified_last_name` — ID.me-verified values only |
| Attempt-link failure treated as non-fatal (silent success) | Returns explicit `warning: "attempt_link_partial"` recovery state; caller can detect and route accordingly |
| `writeAudit()` not awaited on success path | Success audit is now `await writeAudit(...)` |
| `json()` helper missing `Cache-Control: no-store` | Added to all responses in `create-account` |
| `session_token` in response (misleading name for bearer material) | Renamed to `token_hash`; comment updated to clarify it requires immediate exchange via `verifyOtp` |

### Pre-Deployment Checklist

| Step | Status |
|---|---|
| Apply `supabase/migrations/migration_e_onboarding_attempts_v2.sql` | ⬜ Pending David |
| `ONBOARDING_ENCRYPTION_KEY` set on both functions | ✅ Done 2026-06-01 |
| Deploy `idme-verification-start` (`--no-verify-jwt`) | ✅ Deployed 2026-06-01 |
| Deploy `idme-exchange-v2` (`--no-verify-jwt`) | ✅ Deployed 2026-06-01 |
| Deploy `create-account` (`--no-verify-jwt`) | ✅ Deployed 2026-06-01 |
| Codex re-QA against live schema + deployed functions | ⬜ After deploy |

### Commits

| File | Commit |
|---|---|
| `supabase/migrations/migration_e_onboarding_attempts_v2.sql` | 064bffaf4912 |
| `supabase/functions/create-account/index.ts` | 738c257984f5 |

---

## Deployment — 2026-06-01 — David

All three Edge Functions deployed to `wvzjfxacykgsaffskgtr` from remediated GitHub source:

| Function | Status |
|---|---|
| `idme-verification-start` | ✅ Deployed 2026-06-01 — first deployment |
| `idme-exchange-v2` | ✅ Deployed 2026-06-01 — redeployed from remediated source |
| `create-account` | ✅ Deployed 2026-06-01 — redeployed from remediated source |

**Migration E application later verified by Codex QA.**
Live `onboarding_attempts` now includes `state_hash`, `code_verifier_ciphertext`, `consumed_at`, the `account_creating` state, state-aware constraints, and service-role-only table grants.

---

## Codex Re-QA — 2026-06-01 — Second Pass

**Reviewer:** Codex  
**Scope:** TASK-0045 second remediation, Migration E v3, live deployed Edge Functions, live Supabase schema  
**Live Supabase Project Checked:** `wvzjfxacykgsaffskgtr`  
**Live Functions Checked:** `idme-verification-start` v1, `idme-exchange-v2` v5, `create-account` v4  
**Verdict:** Approved for downstream backend work; P2/P3 hardening remains before production launch

### Summary

The P1 blockers from the prior Codex QA are resolved. Migration E v3 is applied live, all three Edge Functions are deployed, and the live schema now supports the server-created attempt/state/PKCE model required for the ID.me-first flow.

TASK-0046 and TASK-0047 may proceed against this trust model. Production launch should still wait for the P2/P3 hardening items below and an end-to-end sandbox run.

### Live State Verified

- `idme-verification-start` is deployed with `verify_jwt = false`.
- `idme-exchange-v2` is deployed with `verify_jwt = false` and now requires `{ attempt_id, code, state }`, not browser-supplied `code_verifier`.
- `create-account` is deployed with `verify_jwt = false` and now atomically claims `id_verified -> account_creating` before `admin.createUser()`.
- `public.onboarding_attempts` now has `state_hash`, `code_verifier_ciphertext`, and `consumed_at`.
- `public.onboarding_attempts.idme_subject` and `id_verification_level` are nullable for pre-callback `started` rows.
- State-aware constraints are present:
  - `started` requires `state_hash` and `code_verifier_ciphertext`.
  - `id_verified`, `account_creating`, and `linked` require `idme_subject` and `id_verification_level`.
  - `linked` requires `profile_id`.
- The active ID.me subject unique index is now durable-state based and no longer uses `expires_at > now()`.
- `anon` and `authenticated` grants on `public.onboarding_attempts` have been revoked; only `service_role` retains table privileges.
- `profiles.onboarding_step` includes the ID.me-first values: `identity`, `confirm`, `license`, `phone`, `plan`, `selfie`, `pass`, `complete`.

### Resolved Previous Findings

- P1 server-side state/PKCE validation is resolved.
- P1 browser-supplied `code_verifier` is removed from the exchange contract.
- P1 `create-account` email trust boundary is improved: `email_confirm` is true only when the submitted email matches `verified_email`.
- P1 account creation now atomically claims the attempt before Auth user creation.
- P1/P2 Migration E incompatibilities are resolved live.
- P2 browser-edited names are no longer used as the verified identity anchor; `create-account` uses `verified_first_name` and `verified_last_name` from the attempt.
- P3 excess grants on `onboarding_attempts` are resolved live.

### Remaining Findings

#### P2 — `idme-exchange-v2` still logs provider diagnostic fields

Function/pattern: `supabase/functions/idme-exchange-v2/index.ts` lines 214-228

What is wrong:

- The function still logs ID.me response-shape diagnostics, including subject-like fields such as `sub` / `uuid`.
- This was useful during provider integration, but it should not remain in production because ID.me subject identifiers and provider response metadata are sensitive operational data.

Required fix:

- Remove diagnostic provider-shape logging before production launch.
- Keep only minimal error logs that do not include ID.me subject, email, phone, name, access token, authorization code, state, or raw provider payload.

#### P2 — Provider response contract remains heuristic

Function/pattern: `supabase/functions/idme-exchange-v2/index.ts` lines 235-257

What is wrong:

- Subject, IAL, name, email, and phone extraction still accepts multiple possible fields/attribute shapes.
- That is acceptable for sandbox stabilization, but production should pin the exact ID.me endpoint/response contract used by PassTo.

Required fix:

- Before production, document the exact ID.me attributes/UserInfo endpoint and required fields.
- Fail closed when the expected production fields are absent or ambiguous.
- Keep the current flexible extraction only as a temporary sandbox compatibility layer or put it behind an explicit environment/provider mode.

#### P2 — `create-account` still returns a token hash to browser JavaScript

Function/pattern: `supabase/functions/create-account/index.ts` lines 288-317

What is wrong:

- The function still returns `linkData.properties.hashed_token` in the JSON body as `token_hash`.
- This is improved from the prior review because the response now uses `Cache-Control: no-store`, no longer calls it a session token, and the attempt model is now server-bound. Still, the token hash is one-time bearer login material and must be treated carefully.

Required fix:

- For production, prefer a backend auth callback that exchanges the token and sets secure, `HttpOnly`, `SameSite` cookies.
- If the SPA token-hash handoff remains the launch path, document the explicit Lovable contract: immediately call `supabase.auth.verifyOtp({ token_hash, type: 'email' })`, do not log/store/cache the token hash, and clear the onboarding attempt reference after session creation.

#### P3 — Public pre-account functions need abuse controls before launch

Function/pattern: `idme-verification-start`, `idme-exchange-v2`, `create-account`

What is wrong:

- These endpoints must be public because they run before Supabase Auth exists.
- CORS is correctly restricted, but CORS is not abuse protection for non-browser callers.

Required fix:

- Add rate limiting or another abuse-control layer before production launch, especially on `idme-verification-start` to prevent attempt-table spam.
- Consider auditing request IP/user-agent if consistent with the privacy model.

### Approval Guidance

- TASK-0046 may proceed: the ID.me-first trust boundary is now sufficient for license binding backend work.
- TASK-0047 may proceed after TASK-0046 defines the durable license/data-match gate.
- Do not treat this as production launch approval. Complete the P2/P3 items above and run an end-to-end ID.me sandbox enrollment before production use.

**Final Verdict:** Approved for downstream backend work with required P2/P3 production hardening.

---

## Codex Third QA — 2026-06-01 — Verdict: Approved

**Reviewer:** Codex
**Live Supabase Project Checked:** `wvzjfxacykgsaffskgtr`
**Verdict:** Approved — pre-production hardening required before launch; does not block TASK-0046/TASK-0047 development

### Validated

- Migration E applied correctly to live schema
- `idme-verification-start`, `idme-exchange-v2`, `create-account` deployed from remediated source
- Server-side PKCE and state validation model approved
- `consumed_at IS NULL` atomic replay guard approved
- Conditional `email_confirm` logic approved
- AES-GCM key handling approved — no plaintext logged
- `constantTimeEqual()` implementation adequate

### Pre-Production Hardening Required (not blocking TASK-0046/TASK-0047)

| # | Item | Status |
|---|---|---|
| H-1 | Remove ID.me diagnostic logging from `idme-exchange-v2` | ✅ Done 2026-06-01 (commit 74af005f6636) |
| H-2 | Pin the exact ID.me provider response contract (field names, shapes) | ⬜ After sandbox run |
| H-3 | Document or replace `token_hash` browser handoff | ⬜ Pre-launch |
| H-4 | Add abuse controls / rate limiting on public pre-account endpoints | ⬜ Pre-launch |
| H-5 | Run end-to-end ID.me sandbox enrollment | ⬜ Pending David |

### What Is Now Unblocked

- **TASK-0046** — License Info Lookup and ID.me/License Binding Backend (schema mapping + provider decision required before Claude executes)
- **TASK-0047** — Phone/Plan/Payment Routing Reorder
- **TASK-0044** — Dev Test Personas seed harness (pending Codex QA of the seed script)
