# TASK-0073 - Define Isolated UAT and Live Nurse Demo Program

**Task ID:** TASK-0073
**Title:** Define Isolated UAT and Live Nurse Demo Program
**Owner:** Codex / David / Claude
**Status:** Implementation Approved - Awaiting Implementation Task Scaffolding
**Priority:** Critical
**Created Date:** 2026-06-15
**Approved Date:** Planning discussion approved 2026-06-15; implementation pending
**PRD Section:** Section 7 - Launch Readiness, QA, and Open Decisions
**Depends On:** TASK-0044 (dev personas/seed safety), TASK-0068 (MVP QA plan),
TASK-0069 (production configuration checklist), TASK-0070 (launch smoke test),
TASK-0072 (wallet issuance)
**Blocks:** Repeatable internal UAT, moderated nurse UAT, reliable live nurse demonstrations
**Protocol:** `docs/tasks/UAT_PROTOCOL_2026-06-15.md`
**Runbook:** `docs/tasks/LIVE_NURSE_DEMO_RUNBOOK_2026-06-15.md`

## Product Goal

Create an honest, repeatable way to test and demonstrate PassTo when fictional
identities cannot complete real ID.me verification and fictional nursing
licenses cannot be looked up through RapidAPI/Propelus.

The program must show the real PassTo product journey without representing
simulated provider results as genuine verification.

## Approved Planning Direction

- Maintain a separate demo/UAT environment with its own Supabase project,
  provider credentials, wallet identifiers, domains, and synthetic data.
- Use two modes on the isolated platform:
  - Demo mode: presenter-controlled Avery Demo golden path.
  - UAT mode: moderated testing, deterministic scenarios, evidence, and review.
- Stamp every session with an immutable `mode = demo | uat` value and expose
  distinct console actions for each mode.
- Begin with internal UAT, then David personally recruits nurses for moderated
  UAT using the fictional Avery Demo identity.
- Demonstrate the full journey from onboarding through wallet installation and
  time-limited sharing.
- Use real SMS OTP where available, to David's phone or the participant's phone.
- Require a fresh selfie as part of the trust cycle.
- Use an acknowledgment screen before simulated identity verification.
- Allow any reasonable alphanumeric license number in the synthetic golden path.
- Let participants keep a clearly marked souvenir wallet pass.
- Keep launch-list opt-in separate from demo identity and UAT records.

## Technical Scope

- Define environment isolation and production fail-closed requirements.
- Define the Avery Demo synthetic identity and license fixture.
- Define simulated identity and license-provider contracts.
- Define real OTP, selfie, credential, wallet, sharing, verifier, and activity
  behavior inside the isolated environment.
- Define the presenter reset/preflight workflow.
- Define moderated internal and nurse UAT procedures.
- Define recording, consent, evidence, timing, intervention, and review rules.
- Define demo pass labeling and non-convertibility requirements.
- Define which agreed product changes require reconciliation with existing PRD,
  security, verifier, wallet, and tier documentation.
- Define how the demo environment is synchronized with production schema, Edge
  Function versions, and frontend contracts before every UAT cohort.
- Define automatic participant-phone cleanup through the presenter console or a
  scheduled backend cleanup job; normal operation must not depend on manual SQL.

## Out of Scope

- Creating the demo/UAT Supabase project or domains.
- Building the provider simulators or presenter console.
- Changing production ID.me, RapidAPI/Propelus, Twilio, wallet, selfie, sharing,
  verifier, or launch-list behavior.
- Using real nurse licenses or representing synthetic records as authentic.
- Promoting demo/UAT records into production.
- Production launch approval.

## Environment Boundary

**Demo/UAT Supabase project ref provided by David:** `atnmcjkjshyqcttnmzkq`

**Initial Lovable preview URL provided by David:** `https://id-preview--95a347a5-9773-4543-a44f-fa135f4d851a.lovable.app/?__lovable_sha=2e520487`

The Lovable URL is a preview/build reference, not the stable demo/UAT domain.
The implementation task must still define the approved demo domain, redirect
URLs, CORS origins, and environment variables before repeatable UAT or demos.

The isolated platform must:

- Use a Supabase project that is not the production PassTo project.
- Use separate Apple pass type and Google Wallet class identifiers.
- Use separate credentials, secrets, storage, logs, and analytics.
- Use reserved synthetic email identities and synthetic licenses.
- Reject production project refs, production provider credentials, and
  production wallet identifiers.
- Mark every record with an immutable environment/session classification.
- Mark every session with one immutable operating mode: `demo` or `uat`.
- Prevent any demo/UAT credential from being promoted into production.
- Require real production onboarding before issuing a useful professional pass.

## Canonical Demo Identity

```text
first_name: Avery
last_name: Demo
license identity: synthetic
license number: participant-entered alphanumeric value
default license type: RN
default state: New York
default status: Active - Simulated
```

The participant's or presenter's selfie may represent Avery Demo for the
session. It does not prove that the photographed person is a nurse or that
ID.me verified Avery Demo.

## Demo Artifact Rules

Every portable demo credential must permanently and prominently display:

```text
DEMO
NOT A VALID PROFESSIONAL CREDENTIAL
Avery Demo
Synthetic license data
```

Additional rules:

- No permanent verifier QR or long-lived verification token.
- Unique serial/object ID per demo session so souvenir passes do not overwrite
  one another.
- Validate that the dedicated Apple pass type, certificate lifecycle, and
  Google Wallet identifiers can support the expected session volume without
  rotation during a UAT cohort.
- No conversion or migration into a production credential.
- Share links retain normal short-lived, first-use behavior.
- A general launch-list link may appear in informational pass content, but it
  must not function as credential verification.
- A dedicated Apple demo pass type ID and signing certificate are hard gates
  before demo wallet issuance. Production wallet identifiers must not be reused.

## Selfie and Verifier Direction

The agreed target behavior is:

- Selfie remains a required production issuance gate.
- Selfie appears in the PassTo app credential experience.
- Verifier view displays the latest selfie available at access time.
- Verifier copy describes it as a credential-holder-provided current likeness,
  not ID.me biometric verification.
- If a selfie is missing or manually deleted, display `Selfie not provided`.
- A concerned UAT participant may ask David to manually delete the selfie.
- Wallet-pass photo rendering remains a separate implementation decision and is
  not required by this protocol.

## Verifier and Activity Direction

- Demo verifier details may be fictional or real.
- Production verifiers must provide accurate information and accept Terms.
- PassTo must not claim verifier identity is independently verified unless a
  later authentication feature actually does so.
- Nurse activity history should show verifier-provided name, organization,
  purpose, and access time where those fields are approved and implemented.
- Such data must be labeled `Verifier-provided information - not independently verified`.

## Source Reconciliation Required

Moderated nurse UAT cannot begin until the following older assumptions are
reconciled:

- `docs/flows/IDME_FIRST_ONBOARDING.md` calls selfie optional in one route table.
- `docs/features/PASS_MANAGEMENT.md` and
  `docs/design_system/WALLET_PASS_DISPLAY_SPEC.md` use tier-dependent selfie
  display rules.
- `docs/flows/VERIFIER_CREDENTIAL_VIEW.md` and the current verifier safe-display
  contract exclude the selfie and organization/purpose fields.
- `docs/architecture/SECURITY_MODEL.md` limits verifier data collection and
  prohibits direct exposure of private selfie storage.

The implementation task must build a backend-authorized, short-TTL selfie
delivery mechanism and update the safe-display contract without exposing
storage paths or broadening anonymous database access. The implementation and
its security behavior must pass Codex QA before moderated nurse cohort 1.

Pre-cohort decisions are divided as follows:

- Fixed before cohort 1: selfie is required for issuance; selfie is visible in
  the app and verifier view; verifier identity is self-declared; demo artifacts
  are permanently invalid; demo and production data never mix.
- Informable by internal Stage A findings: exact disclosure copy, placement and
  prominence of selfie context, presenter workflow, and non-security UI details.
- Any change to a fixed trust/security decision requires source reconciliation
  and approval before it is used in moderated nurse UAT.

Internal technical testing may validate environment isolation, simulators,
wallet signing, and reset mechanics before reconciliation closes. Any such run
is engineering validation, not cohort evidence or product acceptance.

## Session Ownership

Assign these roles before each moderated UAT session:

| Role | Responsibility |
|---|---|
| Moderator | Introduces the session, observes, and uses the intervention ladder. |
| Console operator | Prepares/resets the session and handles technical recovery outside participant interaction. |
| Recording redactor | Completes the post-session redaction checklist before review. |
| Evidence reviewer | Reviews the recording, system timeline, notes, and findings package. |

One person may hold multiple roles, but the assignments must be recorded before
the session. When David moderates alone, console preparation happens before the
participant begins; redaction and evidence review happen after the session.

## Retention Baseline

- Participant phone/contact data: clear at session close, except separately
  consented launch-list data.
- Raw UAT recording: restricted access; delete within 30 days after capture.
- Redacted recording or clips: retain for up to 12 months, then review for
  deletion or renewed documented need.
- Structured, de-identified findings: retain as product research.
- Selfie: retain until replaced or manually deleted at participant request.

## Acceptance Criteria

- [x] UAT protocol defines participants, consent, recording, moderator behavior,
  test stages, metrics, evidence, review, and exit criteria.
- [x] Live-demo runbook defines Zoom and in-person setup, preflight, script,
  recovery paths, reset, souvenir pass, sharing, and launch-list close.
- [x] Both artifacts clearly distinguish simulated provider evidence from real
  PassTo, OTP, selfie, wallet, and sharing behavior.
- [x] Product-controlled timing and external-provider/readiness delays are
  measured separately without a protocol-level duration promise.
- [x] Environment and artifact safeguards prevent demo credentials from being
  mistaken for production credentials.
- [x] David approves the reconciled product/privacy requirements for
  implementation. (Approved 2026-06-16 - APPROVAL-0036.)
- [ ] A separate implementation task defines architecture, schema, RLS,
  storage, simulator, wallet, frontend, and deployment changes. (Creation
  authorized 2026-06-16 - APPROVAL-0036; task not yet scaffolded.)
- [ ] The verifier selfie-delivery contract is implemented, security-reviewed,
  and Codex-QA'd before moderated nurse cohort 1.
- [ ] Demo wallet identifiers and certificate lifecycle are verified for the
  planned cohort/session volume.
- [ ] A separate Apple demo pass type ID and certificate are configured under
  David approval before wallet testing.
- [ ] Demo/production synchronization is verified before each UAT cohort.
- [ ] Participant phone cleanup is automatic or presenter-console-driven and
  verified without manual SQL.

## Implementation Ownership

- Claude leads implementation architecture, backend/environment work,
  integration, deployment evidence, and handoff.
- Lovable owns approved demo/UAT frontend changes and presenter-facing UI.
- Codex owns architecture/security review and QA.
- David owns provider accounts, certificates, approvals, and product decisions.

## David Decisions - 2026-06-15

### Demo Payment and Tier

**Codex recommendation:** Bypass Stripe in demo/UAT and assign a fixed
server-controlled demo entitlement mode. Do not persist an unsupported `demo`
value into the production subscription-tier contract, and do not call the
session Free, Standard, or Premier.

**David Decision:** Approved.

Demo/UAT bypasses Stripe and uses server-controlled demo entitlements outside
the production subscription-tier contract.

### Demo Identity Control

**Codex recommendation:** Replace the production `Continue with ID.me` control
entirely in demo/UAT with `Continue with simulated identity verification`.
Show the acknowledgment immediately before returning the simulated result. Do
not display an actionable ID.me-branded button in the isolated environment.

**David Decision:** Approved.

Demo/UAT replaces the production ID.me button entirely with simulated-identity
language and does not show an actionable ID.me-branded control.

### Target Dates

Record target dates separately for:

- Stage A internal UAT.
- Stage B moderated nurse cohort 1.

**David Decision:** July 1, 2026.

Target date applies to both Stage A internal UAT and Stage B first moderated
nurse cohort unless David later splits the dates.

## QA Plan

- Review both artifacts against PRD Sections 3, 5, and 7.
- Confirm every portable artifact has permanent demo labeling.
- Confirm simulated ID.me and license results require explicit acknowledgment.
- Confirm production credentials and demo records cannot mix.
- Confirm recording and evidence instructions avoid retaining OTPs, raw tokens,
  unrelated notifications, and exposed storage URLs.
- Confirm the selfie/verifier changes receive a security and privacy review
  before implementation.

## David Approval Request

The planning direction was developed with David on 2026-06-15. Separate
approval is required before creating infrastructure, changing product behavior,
deploying simulators, collecting nurse UAT recordings, or exposing selfies in
the verifier experience.

**David Decision:** Approved - Implementation Authorized
**Date:** 2026-06-16
**Approval Record:** APPROVAL-0036

David approved the reconciled product/privacy direction (selfie required at
issuance, latest selfie shown in nurse app and verifier view via
backend-authorized short-TTL delivery without exposed storage paths) and
authorized creation of a separate implementation task defining architecture,
schema, RLS, storage, simulator, wallet, frontend, and deployment changes.

Approval does not authorize provisioning the demo/UAT Supabase project,
domains, certificates, provider credentials, simulator deployment, cohort
recordings, or production launch. Those remain gated on the implementation
task and the pre-cohort-1 gates listed in this document.

## Codex Disposition of Claude Review - 2026-06-15

| Comment | Disposition | Result |
|---|---|---|
| CR-0073-01 | Accept | Added immutable `demo` / `uat` mode requirement. |
| CR-0073-02 | Accept with scope clarification | Moderated nurse UAT is gated on reconciliation; internal engineering validation may proceed but is not cohort evidence. |
| CR-0073-03 | Accept | Selfie delivery contract is required before cohort 1. |
| CR-0073-04 | Accept | Added wallet identifier/certificate volume validation. |
| CR-0073-05 | Accept | Added session ownership matrix; roles may be combined when recorded. |
| CR-0073-06 | Accept with bounded retention | Raw recordings 30 days; redacted material up to 12 months; selfie remains until replaced or manually deleted. |
| CR-0073-07 | Acknowledged | Doc-level comments are dispositioned in their respective artifacts. |

## Codex Disposition of Claude Session 2 Review - 2026-06-15

| Comment | Disposition | Result |
|---|---|---|
| CQ-0073-01 | Corrected | TASK-0068 through TASK-0072 exist; dependency descriptions were added inline. |
| CQ-0073-02 | David approved | Demo/UAT bypasses Stripe and uses server-controlled demo entitlements outside the production tier contract. |
| CQ-0073-03 | David approved | Demo/UAT replaces the ID.me button entirely with simulated-identity language. |
| CQ-0073-04 | Resolved | David assigned TASK-0073 to Claude; ownership boundaries are now explicit. |
| CQ-0073-05 | David approved | Stage A and Stage B target date is July 1, 2026. |
| CR2-0073-01 | Accept | Separate Apple demo pass ID/certificate added as a hard gate. |
| CR2-0073-02 | Accept | Selfie-verifier contract must be implemented and Codex-QA'd before cohort 1. |
| CR2-0073-03 | Accept | Fixed pre-cohort decisions are separated from Stage A-informable presentation details. |
| CR2-0073-04 | Accept | Sync verification is required before each UAT cohort. |
| CR2-0073-05 | Accept | Participant phone cleanup cannot depend on manual SQL. |
| CR2-0073-06 | Accept | Added to top-line UAT measures. |
| CR2-0073-07 | Accept | Cohort 1 is explicitly directional, not definitive. |

---

## Claude Review Comments - 2026-06-15

**Reviewer:** Claude
**Status:** Suggestions and clarifying questions; disposition pending Codex / David
**Scope:** Comments on TASK-0073, the UAT Protocol, and the Live Demo Runbook as
a whole. Doc-specific items are also captured in the respective files.

### CR-0073-01 - Split DEMO and UAT into modes at the artifact level

The runbook currently mixes presenter-polishing guidance ("memorable synthetic
value") with UAT-observation guidance ("don't tell the nurse it can be random").
Without an explicit mode switch tagged at session creation
(`mode: DEMO | UAT`), drift between intent and execution is likely. Recommend
the isolated platform stamp every session record with an immutable `mode` value
and expose distinct presenter-console controls so operators cannot accidentally
apply the wrong instruction to the wrong session.

### CR-0073-02 - Spec reconciliation should gate UAT spin-up, not run alongside it

`Source Reconciliation Required` already lists five docs that contradict the
selfie-required and verifier-visible direction
(`docs/flows/IDME_FIRST_ONBOARDING.md`, `docs/features/PASS_MANAGEMENT.md`,
`docs/design_system/WALLET_PASS_DISPLAY_SPEC.md`,
`docs/flows/VERIFIER_CREDENTIAL_VIEW.md`, `docs/architecture/SECURITY_MODEL.md`).
The UAT protocol and runbook already assume the new behavior. If UAT runs
against an unreconciled spec, findings cannot be cleanly attributed to "product
confusing" vs. "spec changed under us." Recommend either (a) close the
reconciliation task before cohort 1 starts, or (b) explicitly scope cohort 1 as
exploratory-only with non-binding exit criteria.

### CR-0073-03 - Selfie-in-verifier-view backend contract must be scoped before cohort 1

The protocol and runbook depend on a short-TTL, backend-authorized selfie
delivery that does not expose private storage paths (per `SECURITY_MODEL.md`).
TASK-0073 acknowledges this must be designed, but the implementation task has
not been created. If the delivery contract lands mid-cohort, sessions test
different products. Recommend the implementation task define this contract
before cohort 1.

### CR-0073-04 - Apple pass serial-number uniqueness across many sessions

Souvenir-pass-per-session implies a high serial count over time on the demo
Apple cert. Confirm the demo Apple pass type ID and signing environment are
dimensioned for this volume and that rotation is not required mid-cohort.

### CR-0073-05 - Ownership during a UAT session

The protocol does not state who runs the presenter console, triggers session
reset, redacts the recording, or reviews the evidence package when David is the
moderator. If David is moderating, console operation cannot reliably also be
David in real time. Recommend a one-line ownership matrix per session role:
moderator, console operator, evidence reviewer, recording redactor.

### CR-0073-06 - Retention policy for participant PII and recordings

Protocol section 15 says "preserve non-sensitive evidence under session ID" but
does not bound PII retention. Recommend explicit caps now: raw recordings
retained N days then deleted or redacted; redacted clips retained indefinitely;
participant phone cleared at session close (already stated); selfie retained
until replaced or until manual deletion request, whichever comes first.

### CR-0073-07 - Cross-reference: doc-level comments

Doc-specific items are appended to:

- `docs/tasks/UAT_PROTOCOL_2026-06-15.md` - friction metrics, n=5 evidence bar,
  license-number observation, recording redaction, pass/assisted definition,
  real-license-number redirect, in-person guardrail, opt-in as metric,
  simulation acknowledgment in evidence.
- `docs/tasks/LIVE_NURSE_DEMO_RUNBOOK_2026-06-15.md` - production-time
  disclosure line, mode switch, in-person participant-phone guardrail,
  verifier-not-verified disclosure as UAT variable, per-session fallback
  uniqueness, connectivity-fallback recording wording.
