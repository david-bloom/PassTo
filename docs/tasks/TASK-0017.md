# TASK-0017 — Remove Dead Airtable Code from P1, P2, P3

**Task ID:** TASK-0017
**Title:** Remove Dead Airtable Code from P1, P2, P3
**Owner:** David (Lovable editor) + Claude (spec)
**Status:** Not Started
**Priority:** High
**Created Date:** 2026-05-26
**Blocked By:** TASK-0016 (env var switch should precede code cleanup)
**Blocks:** Nothing — this is cleanup, not a dependency gate
**Related Tasks:** TASK-0009, TASK-0016

---

## Product Goal

Remove all dead Airtable integration code from the three Lovable projects, as identified in the TASK-0009 audit (CF-4). This eliminates calls to abandoned services, removes deployment noise from the Lovable-managed Supabase instances, and brings the codebase into alignment with the approved stack (FD-008).

---

## Dead Code Inventory (from TASK-0009 audit)

### P1 — PassTo Website

| Location | Dead Code | Action |
|---|---|---|
| `src/components/AccessGateModal.tsx` (or similar) | `supabase.functions.invoke("sync-airtable", ...)` call | Remove call and any surrounding Airtable-specific logic |
| `src/pages/CreateAccount.tsx` (P1) | `supabase.functions.invoke("sync-airtable", ...)` call | Remove call |
| Lovable-managed Supabase P1 (`zektkbhvmbbmhvthwah`) | `sync-airtable` edge function deployed | Delete via Supabase dashboard (after P1 env vars switched to canonical) |

### P2 — PassTo Enroll

| Location | Dead Code | Action |
|---|---|---|
| `src/pages/CreateAccount.tsx` (P2) | `supabase.functions.invoke("create-airtable-record", ...)` call | Remove call (covered in P2 pipeline migration — TASK-0011 spec §4.1) |
| Lovable-managed Supabase P2 (`ofpxczstptysqxoruiox`) | `create-airtable-record` edge function deployed | Delete via Supabase dashboard (after P2 env vars switched to canonical) |

### P3 — PassTo App

| Location | Dead Code | Action |
|---|---|---|
| `src/components/AccessGateModal.tsx` (P3) | `// Skip Airtable sync for now` comment block + any disabled Airtable code beneath it | Remove comment and dead code block |

---

## Execution Notes

1. **Lovable frontend changes:** David makes changes in the Lovable code editor. Lovable auto-deploys on save. Verify each project after changes.

2. **Edge function deletion from Lovable-managed Supabase:** The Lovable-managed Supabase instances (`zektkbhvmbbmhvthwah`, `ofpxczstptysqxoruiox`) are only accessible via the Supabase dashboard (not via Supabase MCP, which is scoped to David's org). David can delete the edge functions directly:
   - Supabase dashboard → Project `zektkbhvmbbmhvthwah` → Edge Functions → Delete `sync-airtable`
   - Supabase dashboard → Project `ofpxczstptysqxoruiox` → Edge Functions → Delete `create-airtable-record`

3. **Timing:** Edge function deletion from Lovable-managed Supabase can happen any time after TASK-0016 (env var switch), since those instances will no longer receive traffic.

4. **P2 `create-airtable-record` removal:** This overlaps with the P2 pipeline migration spec (TASK-0011 §4.1). If the TASK-0011 implementation task has already been executed, this call may already be removed. Check before making the change.

---

## Verification After Each Change

After removing calls from each project:
- Open browser console on the relevant project URL
- Confirm no errors related to `sync-airtable` or `create-airtable-record`
- Confirm the page that previously called those functions still works correctly (or fails gracefully if dependent logic is also pending migration)

---

## Acceptance Criteria

- [ ] `sync-airtable` calls removed from P1 (`AccessGateModal.tsx`, `CreateAccount.tsx`)
- [ ] `create-airtable-record` call removed from P2 (`CreateAccount.tsx`)
- [ ] Dead Airtable comment block removed from P3 (`AccessGateModal.tsx`)
- [ ] `sync-airtable` edge function deleted from Lovable-managed Supabase P1
- [ ] `create-airtable-record` edge function deleted from Lovable-managed Supabase P2
- [ ] No console errors related to removed Airtable calls
- [ ] P3 `AccessGateModal.tsx` loads cleanly without dead code

---

## David Approval

Not started.

## Recommended Next Task

Section 1 is complete once TASK-0017 is done. Proceed to PRD Section 2.
