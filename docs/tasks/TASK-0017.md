# TASK-0017 — Remove Dead Airtable Code from P1, P2, P3

**Task ID:** TASK-0017
**Title:** Remove Dead Airtable Code from P1, P2, P3
**Owner:** David (Lovable editor) + Claude (spec)
**Status:** Done — 2026-05-26
**Priority:** High
**Created Date:** 2026-05-26
**Blocked By:** TASK-0016 (env var switch should precede code cleanup)
**Blocks:** Nothing — this is cleanup, not a dependency gate
**Related Tasks:** TASK-0009, TASK-0016

---

## Product Goal

Remove all dead Airtable integration code from the three Lovable projects, as identified in the TASK-0009 audit (CF-4). This eliminates calls to abandoned services, removes deployment noise from the Lovable-managed Supabase instances, and brings the codebase into alignment with the approved stack (FD-008).

---

## Execution Summary

### P1 — PassTo Website (`passtodigital.com`)

Removed via Lovable AI:
- `src/components/AccessGateModal.tsx` — removed supabase import, loading state, 21-line try/catch block wrapping `sync-airtable` invocation, button `disabled={loading}` state
- `src/pages/CreateAccount.tsx` — removed background `sync-airtable` call (18 lines + comment)
- `supabase/functions/sync-airtable/` — entire directory deleted; deployed edge function removed from Lovable-managed Supabase

### P2 — PassTo Enroll (`enroll.passtodigital.com`)

- `src/pages/CreateAccount.tsx` — `create-airtable-record` call already removed during TASK-0016 ✅
- `supabase/functions/create-airtable-record/` — directory did not exist in P2 project
- Deployed edge function — not present on P2 Lovable-managed Supabase

### P3 — PassTo App (`app.passtodigital.com`)

Removed via Lovable AI:
- `supabase/functions/create-airtable-record/index.ts` — deleted; deployed edge function removed from Lovable-managed Supabase
- `src/components/AccessGateModal.tsx` — no Airtable code in component; only reference was in the deleted function directory

---

## Acceptance Criteria

- [x] `sync-airtable` calls removed from P1 (`AccessGateModal.tsx`, `CreateAccount.tsx`)
- [x] `create-airtable-record` call removed from P2 (`CreateAccount.tsx`) — done in TASK-0016
- [x] Dead Airtable function source removed from P3 (`supabase/functions/create-airtable-record/`)
- [x] `sync-airtable` edge function deleted from Lovable-managed Supabase P1
- [x] `create-airtable-record` edge function deleted from Lovable-managed Supabase P3
- [x] P1 smoke test passed — no console errors
- [x] P3 smoke test passed — no console errors

---

## David Approval

Done — 2026-05-26.

## Recommended Next Task

Section 1 is complete. Proceed to PRD Section 2.
